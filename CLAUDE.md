# ExpiryMate — Project Memory

## Standing Instructions
- **Always update `docs/ai-use-log.md` at the end of every session.** Add a dated entry (DD/M/YY format) summarising what was discussed or built. If multiple things happened, use a short paragraph or bullet points. Do this without being asked.
- **Always update `docs/decisions.md` whenever a design or architectural decision is made.** Record what was decided and why. Do this without being asked.
- **Commit and push to GitHub after every milestone.** A milestone means a completed slice, or anything finer-grained that's a coherent, working unit (e.g. one component wired up, one migration applied, one doc written). Each commit should have a message describing what that specific unit of work was — not a generic "updates" message. Push immediately after committing, don't batch multiple milestones into one push.

## What This Is
A **React + TypeScript + Vite** web app that helps household members track food expiry dates, reducing waste. Users can add items with expiry dates, get status indicators (fresh / expiring soon / expired), edit items, and delete them. Authentication is handled via Supabase Auth.

Built for Year 12 Software Engineering, Assessment Task 3 (Software Engineering Project) — due 8:00am Monday 24 August 2026. Full assessment requirements are reconciled against the repo in `docs/decisions.md` (see "Repository structure alignment" and "Marking-alignment review", both 18/8/26).

## Tech Stack
| Layer | Technology |
|---|---|
| Frontend | React 19, TypeScript 6, Vite 8 |
| Backend / DB | Supabase (Postgres + Auth + RLS) |
| Hosting | Vercel — live at https://expiry-mate.vercel.app, auto-deploys from `main` |
| Styling | Plain CSS custom properties (no component/utility CSS library) |
| Animation | `framer-motion` (added 21/8/26, UI feedback pass three) |
| Build/Lint | ESLint, TSC |

## Environment Variables (`.env.local`)
```
VITE_SUPABASE_URL=https://xsvzsghbmqrhwvjkbqka.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_TTty4_mGdp_FQStC5ZmFpA_1MCPsAwo
```
Not committed (git-ignored). This project is submitted as a zip to a teacher rather than cloned by other developers, so there's deliberately no `.env.local.example` — see `docs/ai-use-log.md`, 18/8/26.

## Project Structure
```
docs/
  01-problem-statement.md
  02-requirements.md
  03-architecture.md
  04-data-model.md
  05-security-review.md
  06-front-end-architecture.md
  07-evaluation.md            — skeleton, fill in once Slices 1-3 are usable
  08-test-plan.md             — skeleton, fill in as tests/ gets populated
  09-iteration-log.md         — skeleton, log UAT/deploy feedback here as it happens
  10-feedback-sprint.md       — Feedback Sprint (teacher UX review, 21/8/26): feedback, discussion, plan of attack, status
  wireframes/
    feedback-sprint-dashboard-wireframe.html — rough, clickable, self-contained mockup (mock 112-item dataset); not wired to Supabase, agree structure before real build
  decisions.md                — slice plan, algorithm spec, all major decisions
  ai-use-log.md               — every substantive AI interaction, prompt/response/accepted-or-not
src/
  App.tsx               — session-managed shell: Auth vs. AuthenticatedApp, imports App.css; AuthenticatedApp owns view-routing state and renders Sidebar + the active view
  main.tsx              — React entry point, imports index.css
  index.css             — reset, base typography, colour variables incl. --color-sidebar-* (Slice 4; Feedback Sprint redesign 21/8/26)
  App.css               — layout for auth screen, sidebar, dashboard, forms, item list/cards (Slice 4; Feedback Sprint redesign 21/8/26)
  hooks/
    useItems.ts         — centralises all Supabase reads/writes for items
    useTheme.ts          — manual light/dark toggle, persisted to localStorage (Slice 4)
    useAnimatedModal.ts — shared "grows from where you clicked" open/close/animation state (Slice 4)
    useExpiryNotifications.ts — finds items within 7 days of adjusted expiry, fires one batched browser Notification per load (Slice 5)
    useItemsWithStatus.ts — memoised computeStatusInfo over an item array (Feedback Sprint real-UI-build, 21/8/26)
  lib/
    supabase.ts         — Supabase client initialisation (anon key only)
    validateItem.ts     — shared client-side validation for add + edit forms
    adjustedExpiry.ts   — getAdjustedExpiry (Slice 3 algorithm), getDaysRemaining, getExpiryStatus
    itemStatus.ts       — computeStatusInfo/BadgeStatus/STATUS_LABEL/STATUS_CLASS, shared by ItemList and useExpiryNotifications (Slice 5)
    guessCategory.ts    — keyword-based category guess from item name, no AI/network call (Slice 6)
    dashboardStats.ts   — countByStatus/countByStorage/sortByUrgency/buildNeedsAttention, shared by Dashboard/PlaceDashboard/ItemList (Feedback Sprint real-UI-build, 21/8/26)
    formatNameList.ts   — shared name-list truncation (5 + "and N more"), used by ExpiryBanner and useExpiryNotifications (Feedback Sprint redesign, 21/8/26)
  components/
    Auth.tsx            — Email/password sign-up & sign-in form
    AddItemForm.tsx     — full item schema, category/storage/quantity/opened/date-opened, opens from App.tsx's "+" button (Slice 4)
    AnimatedModal.tsx   — shared portal/backdrop/escape/scroll-lock modal shell, used by both the add-item and item-detail flows (Slice 4)
    ItemList.tsx        — full item schema, adjusted-expiry status badges/countdown, toolbar (search/filter/sort), card/table view toggle
    ExpiryBanner.tsx    — always-visible in-app fallback for Slice 5's notification (browser Notification can be denied/blocked/missed)
    Dashboard.tsx       — landing view: gapless stat tiles, one Donut + one BarChart, needs-attention list, quick-nav tiles (Feedback Sprint, 21/8/26)
    PlaceDashboard.tsx  — scoped stat-tiles + needs-attention view for one storage location (Feedback Sprint real-UI-build, 21/8/26)
    Donut.tsx           — CSS conic-gradient donut with a real punched centre showing the segment total (Feedback Sprint, 21/8/26)
    BarChart.tsx        — plain HTML/CSS bar chart, reuses Donut's segment shape (Feedback Sprint redesign, 21/8/26)
    Sidebar.tsx          — persistent left nav: profile block, 5 destinations, theme/sign-out footer (Feedback Sprint redesign, 21/8/26)
    icons.tsx           — PlaceStatusIcon + CategoryIcon, pulled out of ItemList.tsx (Feedback Sprint real-UI-build, 21/8/26)
supabase/
  migrations/
    20260507000000_create_items.sql              — items table + RLS policy
    20260601000000_add_item_fields.sql           — adds category, storage_location, quantity, is_opened, date_opened
    20260818000000_split_items_rls_policies.sql  — RLS: one broad policy -> four per-operation policies
    20260818010000_add_items_index.sql           — composite index on (user_id, expiry_date)
    20260818020000_add_eggs_category.sql         — adds Eggs to the food_category enum
  functions/           — empty; Slice 5's optional email-digest stretch was deliberately not built (needs an external email-provider API key + scheduling — see decisions.md, "Slice 5: Expiry Notifications")
  seed/
    seed.sql           — ~20 representative household items for local testing; moved out of migrations/ (18/8/26) so a migration-runner never replays it against production — see decisions.md, "GitHub deploy integration: migration history reconciliation"
    feedback-sprint-seed.sql — 135 items across every category/storage/status (Feedback Sprint, 21/8/26), generated by inverting the adjusted-expiry algorithm per row and verified forward before writing — see decisions.md, "Feedback Sprint database seed". Not yet run.
tests/
  unit/                — empty, reserved — adjustedExpiry.ts is written pure/testable, tests not yet written
  integration/         — empty, reserved for RLS/constraint tests
  smoke/               — empty, reserved for post-deploy manual checklist
```

## Database Schema
```sql
items (
  id               uuid PK   default gen_random_uuid(),
  user_id          uuid FK   → auth.users(id) ON DELETE CASCADE,
  name             text      NOT NULL,
  category         food_category NOT NULL,  -- Dairy|Eggs|Meat|Seafood|Produce|Bakery|Frozen|Beverages|Condiments|Snacks|Leftovers
                                             -- Eggs added 18/8/26 (migration 20260818020000, run against the live project and confirmed)
  storage_location storage_location NOT NULL,  -- Fridge|Freezer|Pantry
  expiry_date      date      NOT NULL,
  quantity         integer   NOT NULL check (quantity between 1 and 999),
  is_opened        boolean   NOT NULL default false,
  date_opened      date,     -- required when is_opened = true
  created_at       timestamptz default now()
)
-- RLS: users can only see/manage their own rows. The original broad "for all"
-- policy was split into four per-operation policies
-- (20260818000000_split_items_rls_policies.sql), run against the live project
-- and confirmed 18/8/26 (four separate policies now visible in the dashboard).
```

**Migration application status (as of 18/8/26):** `20260507000000`, `20260601000000`, `20260818000000` (RLS split), and `20260818020000` (Eggs) have all been run by hand in the Supabase SQL Editor and are live. `20260818010000` (the `(user_id, expiry_date)` index) has **not** been run yet — it's the one outstanding migration. All 5 were applied outside the Supabase CLI/GitHub integration, which matters if that integration ever gets connected: see `decisions.md`, "GitHub deploy integration: migration history reconciliation," for why its own migration-history table needs a manual `supabase migration repair` for the 4 already-applied ones before it's safe to connect.
Full schema detail and ERD: `docs/04-data-model.md`.

## Key Components

### `Auth.tsx`
- Toggle between Sign In / Sign Up
- Uses `supabase.auth.signInWithPassword` and `supabase.auth.signUp`
- Shows confirmation message after sign-up (email verification is confirmed ON in the Supabase project as of 18/8/26)
- Password field has a show/hide toggle (added Feedback Sprint UI feedback pass two, 21/8/26) — a plain-SVG eye/eye-slash icon button inset in the field, flipping the input's `type` between `password`/`text`; `aria-label`/`title` flip between "Show password"/"Hide password" too

### `useItems.ts` (`src/hooks/`)
- Owns all Supabase reads/writes for `items` — `AddItemForm` and `ItemList` no longer talk to Supabase directly
- Returns `{ items, loading, error, addItem, updateItem, deleteItem, refetch }`

### `validateItem.ts` (`src/lib/`)
- `validateItemForm(values)` — shared client-side validation used by both the add form and the inline edit form

### `AddItemForm.tsx`
- Props: `onAdd: (input: ItemInput) => Promise<{ error?: string }>`
- Collects all required fields: name, category, storage location, printed expiry date, quantity, opened status, date opened (conditionally) — every field has its own visible `field-label` title
- No longer rendered inline on the dashboard (Slice 4 fifth revision) — `App.tsx` opens it inside `AnimatedModal` from a fixed "+" button
- Category auto-guessed from the name as it's typed (`guessCategory`, Slice 6) until the user picks one themselves, tracked via a `categoryTouched` flag; date field defaults to today instead of blank (Slice 6), still fully editable

### `useAnimatedModal.ts` / `AnimatedModal.tsx`
- Shared open/visible/origin state machine + portal/backdrop/escape/scroll-lock shell, extracted once the add-item flow needed the same "grows from where you clicked" animation as the item-detail modal
- `useAnimatedModal()` returns `{ open, visible, origin, openFrom, close }`; `openFrom(sourceEl)` captures `sourceEl.getBoundingClientRect()` as the animation's origin point
- Used by both `App.tsx` (add-item modal) and `ItemList.tsx` (item-detail modal)

### `adjustedExpiry.ts` (`src/lib/`)
- `getAdjustedExpiry(item)` — pure function implementing the full Adjusted Expiry Date Algorithm (`decisions.md`) for all 11 categories, returns `{ safe: true, adjustedDate }` or `{ safe: false, message }` for unsafe/not-recommended combinations
- `getDaysRemaining(adjustedDate)`, `getExpiryStatus(days)` — derive the Fresh/Soon/Expired status from the adjusted date, not the printed one

### `itemStatus.ts` (`src/lib/`)
- `computeStatusInfo(item)` — combines `getAdjustedExpiry` + `getDaysRemaining`/`getExpiryStatus` into one `{ result, badgeStatus, daysRemaining, countdownValue, countdownLabel }`, where `badgeStatus` is `'fresh' | 'soon' | 'expired' | 'warning'`
- Extracted from `ItemList.tsx` in Slice 5 so `useExpiryNotifications` uses the identical status logic rather than a second copy
- `daysRemaining` (signed, added Feedback Sprint real-UI-build 21/8/26) is distinct from `countdownValue` (`Math.abs(daysRemaining)`, used for display) — needed wherever the sign itself matters: urgency-sorting, and the list-view table's compact "3d left"/"2d ago" labels

### `dashboardStats.ts` (`src/lib/`) — added Feedback Sprint real-UI-build (21/8/26)
- `STORAGE_LOCATIONS`, `countByStatus`, `countByStorage`, `sortByUrgency` (unsafe items first, then ascending by `daysRemaining`), `buildNeedsAttention(entries, limit)` (unsafe → soon-ascending → expired-most-recent, capped)
- Shared by `Dashboard`, `PlaceDashboard`, and `ItemList` so all three agree on sort/count/priority logic rather than each keeping its own copy

### `useItemsWithStatus.ts` (`src/hooks/`) — added Feedback Sprint real-UI-build (21/8/26)
- `useItemsWithStatus(items)` — memoised `{ item, ...computeStatusInfo(item) }[]`, used by `Dashboard`, `PlaceDashboard`, and `ItemList`

### `icons.tsx` (`src/components/`) — added Feedback Sprint real-UI-build (21/8/26)
- `PlaceStatusIcon` (Fridge/Freezer/Pantry/expired/soon/fresh/warning) and `CategoryIcon` (all 11 food categories) — pulled out of `ItemList.tsx`, where they used to be defined inline, so the Dashboard's quick-nav tiles and section headings can reuse the same SVGs

### `Donut.tsx` (`src/components/`) — added Feedback Sprint real-UI-build (21/8/26); given a real centre hole in the reference-matched redesign (21/8/26); rebuilt as a real SVG ring in UI feedback pass four (21/8/26)
- A real `<svg>` ring — one `<motion.circle>` per segment (`stroke-dasharray`/`stroke-dashoffset`, `stroke-linecap="round"` for rounded data-ends, a small length trim between segments so the card's own background shows through as a real gap) — replacing the earlier CSS `conic-gradient` disc, which had no way to gap or round segments. Segments sweep in on mount with a small stagger. An always-visible text legend (swatch + label + count per segment) sits beside the ring
- `holeSize` (60% of the outer diameter) punches a same-surface-colour circle out of the middle, showing the segment total (`holeUnit` prop, default `"items"`) by default. Hovering a segment (or its linked legend row — the two share one `hovered` key) thickens that segment and crossfades the centre to that segment's own count/label instead (`AnimatePresence mode="wait"`, `.donut-hole-inner`); a `.donut-legend-row-active` class highlights the matching legend row
- **`.donut-row` fills its card's full width (UI feedback pass six, 21/8/26)** — `justify-content: space-between` anchors the ring left and the legend right (previously both hugged the left edge, leaving a large blank block on wide cards); the dashboard also renders it at `size={168}` rather than the component's own 140px default, so the card's height better matches its "Items by location" neighbour. The mobile breakpoint that stacks the row into a column needed its own `justify-content: flex-start` override, since `space-between` acts on the vertical axis once the direction flips

### `BarChart.tsx` (`src/components/`) — added Feedback Sprint reference-matched redesign (21/8/26); redesigned to match the Donut's polish in UI feedback pass four (21/8/26); gradient fill + hover brightness added in UI feedback pass seven (21/8/26)
- Plain HTML/CSS bar chart (no chart library), reusing `Donut.tsx`'s `DonutSegment` shape. Replaces the dashboard's second donut (storage-location breakdown) — per the dataviz skill, comparing a magnitude across a handful of discrete categories is a bar chart's job, not a donut's
- Each bar is directly labelled with its exact value above it and its category below it, so per the skill's ≤4-series rule no separate legend is needed; reuses the already-validated Fridge/Freezer/Pantry categorical colours, no new palette introduced
- Bars grow in from zero height on mount with a small per-bar stagger; a `--color-surface-alt` rail sits behind each bar so even the shortest bar reads as part of the chart rather than blank card background, with a baseline rule under the row. Hovering a bar's column dims its neighbours and scales up its own value label, mirroring the Donut's ring/legend hover pairing
- **Richer bar fill, same underlying colours (UI feedback pass seven, 21/8/26)** — checked the bundled dataviz skill's reference palette and confirmed the existing `--color-place-*` hues are already byte-identical to its validated 3-series categorical set, so "nicer colours" was addressed by rendering, not a hue swap: each segment's colour is now handed to CSS as a `--bar-color` custom property, and `.bar-chart-fill` turns it into a `linear-gradient()` (lighter top, darker bottom, via `color-mix(in srgb, var(--bar-color) …%, white/black)`) plus a `filter: brightness(1.08)` on hover — a box-shadow glow was rejected since `.bar-chart-track`'s `overflow: hidden` would clip it

### `ItemDetailModal.tsx` (`src/components/`) / `useItemDetail.ts` (`src/hooks/`) — extracted from `ItemList.tsx` in UI feedback pass four (21/8/26)
- `CardHero`, `ItemDetailModal`, and its `EditState` type were pulled out of `ItemList.tsx` (unchanged in behaviour — a pure extraction) so other callers can render the same expanded-item view `ItemList`'s cards/table rows already used
- `useItemDetail(onUpdate, onDelete)` holds the open/edit/delete state machine (`expandedId`, `editingId`, `edit`, `error`, `modal` from `useAnimatedModal`, plus `openItem`/`closeItem`/`startEdit`/`cancelEdit`/`saveEdit`/`handleDelete`/`setEdit`) that `ItemList.tsx` used to own as five separate `useState`s inline. `ItemList`, `Dashboard`, and `PlaceDashboard` each call their own instance, so an item opened from a card, a table row, or an `AttentionPanel` row all get the identical modal with identical edit/delete behaviour

### `Sidebar.tsx` (`src/components/`) — added Feedback Sprint reference-matched redesign (21/8/26); manual collapse/expand added UI feedback pass two (21/8/26)
- Persistent left navigation, replacing the old top `.app-header`: a brand mark, a profile block (avatar initials + a name derived from the signed-in email's local part — the schema has no display-name field, see `decisions.md`), five nav destinations (Dashboard/Fridge/Freezer/Pantry/All items) with active-state highlighting via an `activeNav` prop computed in `App.tsx`, and a footer with the theme toggle and sign-out
- Deliberately a constant dark navy in both light and dark theme (new `--color-sidebar-*` tokens in `index.css`, not redefined under `[data-theme='dark']`) — matches the reference's own sidebar staying dark regardless of the content area's theme
- Collapses to an icon-only horizontal top bar below 900px (labels hidden via CSS, `aria-label`/`title` kept on every button so nothing becomes unlabelled) rather than a hamburger drawer
- Also independently collapsible at desktop width via a chevron toggle button, persisted to `localStorage` (`expirymate-sidebar-collapsed`) the same way `useTheme.ts` persists the theme — collapsed, the sidebar narrows to 76px showing only icons (brand mark, avatar, nav icons, footer icons), every item keeping its `aria-label`/`title`. An equal-specificity `.sidebar.collapsed` override inside the 900px media query prevents the desktop-collapsed 76px width from leaking into the mobile full-width layout

### `Dashboard.tsx` / `PlaceDashboard.tsx` (`src/components/`) — added Feedback Sprint real-UI-build (21/8/26); charts row revised in the reference-matched redesign (21/8/26); stat tiles made clickable and charts-row layout tightened in UI feedback pass two (21/8/26); stat tiles reworked to expand `AttentionPanel` in place, "Jump to a place" removed in UI feedback pass three (21/8/26); centred/capped, shared item-detail modal wired in, stat-tile hover bug fixed in UI feedback pass four (21/8/26); `buildNeedsAttention` call uncapped in UI feedback pass seven (21/8/26)
- `Dashboard` is the app's landing view: gapless stat tiles (one accent tile), a charts row of exactly one `Donut` (status breakdown) and one `BarChart` (items by storage location) plus the `AttentionPanel`. The by-storage-location donut from the real-UI-build is gone — its same `countByStorage` data now feeds the bar chart instead. The "Jump to a place" quick-nav row (added in the real-UI-build) is removed — `Sidebar.tsx` already covers Dashboard/Fridge/Freezer/Pantry/All items permanently, so it was a redundant second path to the same destinations
- The donut and bar chart are nested into one flex-column wrapper (`.dash-charts-col`) beside `AttentionPanel`, in a 2-column `.dash-charts-row` — two short cards' combined height approximates the tall card's height, closing the visible empty-space gap a flat 3-column layout left (UI feedback pass two, 21/8/26)
- `PlaceDashboard` is a smaller, scoped version (stat tiles + `AttentionPanel` only, no charts) for one storage location — reached either via the sidebar or (from the main dashboard) clicking a storage-location destination, so the user never jumps straight from the dashboard to the full list without an intermediate summary
- **Stat tiles no longer navigate on click (UI feedback pass three, 21/8/26)** — each is now a `motion.button` (framer-motion, `whileHover`/`whileTap`) holding local `selected: BadgeStatus | 'all' | null` state; clicking toggles which status is "expanded" (clicking the same tile again, or the panel's close button, collapses back to the default view). `onSelectStatus: (status: BadgeStatus | 'all') => void` — still owned by `App.tsx`, wired to `navigateToStatus(status)` (global, from `Dashboard`) or `navigateToStatus(status, place)` (scoped, from `PlaceDashboard`) — is now called only from `AttentionPanel`'s "See full list" button, not from the tile click itself
- **Stat-tile hover no longer lifts the tile (UI feedback pass four, 21/8/26)** — `tileProps` dropped `whileHover: { y: -3 }`, since the tiles sit flush with no gap between them and lifting one revealed the row's edge underneath it and dragged its `.stat-tile-active` ring away with it. Hover feedback is CSS-only now (see `App.css` below); only `whileTap` stays motion-driven
- Both components now take `onUpdate`/`onDelete` (the same functions `ItemList` already received from `useItems` via `App.tsx`) and call `useItemDetail(onUpdate, onDelete)`, rendering the shared `ItemDetailModal` for whichever item `AttentionPanel` reports opened — see `ItemDetailModal.tsx`/`useItemDetail.ts` above
- `.dash-grid` (both components' outer wrapper) is capped at `max-width: 1100px` and centred (`margin: 0 auto`) as of UI feedback pass four (21/8/26) — visible on wide monitors once `.app-main`'s own available width after the sidebar exceeds 1100px; a no-op at narrower widths where it already doesn't
- `App.tsx` owns the actual view-routing state (see below), and now also derives `activeNav` from that same state for `Sidebar`
- **The charts column and `AttentionPanel` now fill a full viewport height with no gap (UI feedback pass seven, 21/8/26)** — `App.tsx` gives `<main>` a new `app-main-fill` class only for these two views, and `flex: 1; min-height: 0` propagates that definite `height: 100vh` down through `.dash-grid`/`.dash-charts-row`/`.chart-card`/`.attention-card`; reverted below the existing 900px breakpoint, where `.app-shell` switches to a column layout and mobile/tablet keep their previous natural page-scroll behaviour. See `App.css`'s own comments on `.app-main-fill` for why `.app-shell`'s pre-existing `min-height: 100vh` alone wasn't enough (confirmed with a Playwright harness, not just reasoned)
- **`buildNeedsAttention(withStatus, 8)` → `buildNeedsAttention(withStatus, withStatus.length)` (UI feedback pass seven, 21/8/26)** in both components — the panel now scrolls internally instead of truncating to 8 rows, so every item that needs attention should be in the list it's given
- **Bug fix — `.stat-row` was being squashed, clipping the tile numbers (UI feedback pass seven, 21/8/26):** caught from a live screenshot right after the above shipped. `.stat-row`'s `overflow: hidden` (pre-existing, for its rounded-corner clipping trick) disables a flex item's usual "never shrink below your content" protection, so once `.dash-grid` had a definite capped height, `.stat-row` (a normal content-based flex-basis, next to `.attention-card`'s `flex-basis: 0%`) absorbed 100% of any shortfall by being silently squashed below its tiles' real height — confirmed happening at every viewport height tested, not just short ones. Fixed with `flex-shrink: 0` on `.stat-row`, so any shortfall correctly lands on `.attention-card` instead

### `AttentionPanel.tsx` (`src/components/`) — added UI feedback pass three (21/8/26); rows made clickable in UI feedback pass four (21/8/26); fills full height and scrolls internally in UI feedback pass seven (21/8/26)
- Shared by `Dashboard` and `PlaceDashboard`, replacing each one's own inline "Needs attention" card markup. Renders either the default needs-attention list, or — when a stat tile is selected — that status's full urgency-sorted list (`filterByStatus`, `dashboardStats.ts`), with a header close button to collapse back
- Animated with framer-motion: `AnimatePresence` (`mode="wait"`) crossfades between the two content states, and each row fades in on mount. The tile currently expanded gets a `.stat-tile-active` CSS ring so the tile↔panel connection stays visible after the click
- Takes ready-made title strings (`defaultTitle`, `selectedTitle`, via `dashboardStats.ts`'s `statusPanelTitle`) and button labels/callbacks from the caller rather than deriving them itself, so `Dashboard` and `PlaceDashboard` can each supply their own scoped wording (e.g. "Needs attention" vs. "Needs attention in Fridge") through the same component
- **Rows are real clickable buttons (UI feedback pass four, 21/8/26)** — each row is now a `motion.button` (`.attention-row-button`, `whileHover`/`whileTap`) calling a new `onOpenItem(item, sourceEl)` prop, wired by the caller to its own `useItemDetail().openItem` — clicking a row opens the exact same `ItemDetailModal` a card or table row does
- **Fills the card's full height and scrolls internally instead of animating its own height (UI feedback pass seven, 21/8/26)** — the outer `motion.div`'s `layout` prop (which used to animate the card's height as its content changed size — 8 capped rows vs. up to 10 vs. empty) is gone; it's now a plain `<div className="attention-card">` whose height is fixed externally by the stretch chain above. Each state's content is wrapped in a new `.attention-panel-content` (`flex: 1; min-height: 0`), and `.attention-list` itself scrolls (`flex: 1; overflow-y: auto; min-height: 0`) while the "See full list" button (`flex-shrink: 0`) stays pinned below it. The old 10-row cap and "and N more" note are gone entirely — `selectedItems` now renders in full, since scrolling handles overflow instead of truncation

### `useExpiryNotifications.ts` (`src/hooks/`)
- `useExpiryNotifications(items, loading)` — returns `{ expiringItems, dismissed, dismiss }`
- `expiringItems`: every item whose `badgeStatus` is `soon` or `expired` (i.e. within 7 days of, or past, its adjusted expiry date); `warning`/unsafe items are deliberately excluded (no adjusted date to be "within 7 days" of, and already always shown at the top of the dashboard)
- Fires exactly one batched browser `Notification` the first time `loading` becomes `false` (guarded by a ref so it never re-fires on later item changes), requesting permission if not yet granted/denied; listing names via the shared `lib/formatNameList.ts` helper (5 names, then an "and N more" suffix) — the same helper `ExpiryBanner` now uses too, added in the reference-matched redesign (21/8/26)
- The optional Slice 5 stretch (a Supabase Edge Function daily email digest) was deliberately not built — see `decisions.md`, "Slice 5: Expiry Notifications"

### `ExpiryBanner.tsx` (`src/components/`) — name-list cap fixed in the reference-matched redesign (21/8/26); removed from `App.tsx`'s render tree in UI feedback pass two (21/8/26)
- Props: `items: ExpiringItem[]`, `onDismiss: () => void`
- Used to inline every expiring item's name with no limit, which is what made it "too crowded and huge" at high item counts (reported directly against the 135-item Feedback Sprint seed) — then capped via `lib/formatNameList.ts`'s 5-name-plus-"and N more" rule, shared with `useExpiryNotifications.ts`'s browser-Notification body
- **No longer rendered anywhere (21/8/26)** — the Dashboard's own "Needs attention" card already surfaces the same items on every page load, so the banner became a second UI answering the same question. `App.tsx` still calls `useExpiryNotifications(items, loading)` for its side effect (the one batched browser `Notification`), just without capturing or rendering its `expiringItems`/`dismissed`/`dismiss` return values. The file and `formatNameList.ts` are left on disk unused rather than deleted — the tooling available in this session can edit/create files on the user's machine but not delete them; `git rm src/components/ExpiryBanner.tsx` is the optional manual cleanup step

### `formatNameList.ts` (`src/lib/`) — added Feedback Sprint reference-matched redesign (21/8/26)
- `formatNameList(names, max = 5)` — full list if `names.length <= max`, else the first `max` plus an "and N more" suffix. Pulled out so `ExpiryBanner` and `useExpiryNotifications` share one truncation rule instead of each having its own (or, as it turned out, only one of them having one at all)

### `guessCategory.ts` (`src/lib/`)
- `guessCategory(name)` — pure keyword lookup, no AI/network call; returns a `FoodCategory` or `null` if nothing matches
- Checked in a deliberate priority order (Frozen first, so "frozen chicken" reads as Frozen not Meat); whole-word regex matching for single-word keywords, plain substring matching for phrases like "ice cream"
- Used by `AddItemForm` to pre-fill `category` from the name, only while the user hasn't picked a category themselves

### `ItemList.tsx`
- Props: `items`, `onUpdate`, `onDelete` (all from `useItems`, passed down via `App.tsx`), plus `initialStorageFilter`/`initialStatusFilter` (Feedback Sprint real-UI-build, 21/8/26) so arriving here via a dashboard "View all X" link opens already scoped, rather than making the user reselect what they just clicked into. No more `loading` prop — `App.tsx` now gates loading at the top level, above all three views, not just this one
- **Its item-detail modal and open/edit/delete state moved out to `ItemDetailModal.tsx`/`useItemDetail.ts` (UI feedback pass four, 21/8/26)** — `ItemList` now calls `useItemDetail(onUpdate, onDelete)` instead of owning five separate `useState`s inline; behaviour is unchanged, this was a pure extraction so `Dashboard`/`PlaceDashboard` could reuse the same modal from `AttentionPanel`'s rows
- Collapsed cards (`ItemCard`) are minimal and click-to-expand: status badge, a large countdown (days left/ago, or a warning label if the item has no safe adjusted date), a category icon (`CategoryIcon`, now in `components/icons.tsx`) beside the name, and the name — no Edit/Delete on the collapsed card. Same category icon also shown in the modal heading and next to the "Category" stat value, for consistency
- Clicking a card (or, since the Feedback Sprint real-UI-build, a table row in list view) opens `ItemDetailModal` via the shared `AnimatedModal` (rendered via `createPortal` into `document.body`), centred, with a "grows from where you clicked" transform animation built from `getBoundingClientRect()` + CSS transitions (no animation library). Shows every remaining fact as its own stat tile, plus Edit/Delete as icon buttons; Edit swaps the modal's content to the inline-edit form in place, with every field now having its own visible `field-label` title. Delete arms an in-place confirmation step (Cancel/Delete, with any failure shown inline) rather than deleting immediately or using a native browser confirm
- Status/countdown derive from `getAdjustedExpiry`'s adjusted date via `useItemsWithStatus(items)` (`src/hooks/`, extracted Feedback Sprint real-UI-build 21/8/26 so `Dashboard`/`PlaceDashboard` share the same memoised computation), not recomputed per-card
- **Rewritten for the Feedback Sprint real-UI-build (21/8/26):** the old by-status/by-place grouped sections and sort-mode toggle are gone. Items now always render as one flat list sorted by `sortByUrgency` (unsafe first, then ascending by days) — the dashboard's stat tiles/needs-attention list now cover the "what needs attention" job the grouped sections used to do. A `displayMode` toggle (`Card view` / `List view`) switches between the card grid and a new `<table>` view (Name/Days left/Status always visible, Category/Storage/Expiry date/Qty/Opened collapse on mobile via a `col-collapsible` class); every table row is clickable/keyboard-activatable and opens the same `ItemDetailModal` cards use, rather than a separate inline expand-row. The status filter dropdown gained a fourth value, `'attention'` ("Needs attention (not fresh)") — soon + expired + warning combined — used both by the dashboard's "View all expiring" link and as a normal selectable filter
- **Defaults to `'table'` (list view), not `'card'` (UI feedback pass five, 21/8/26)** — a one-line change to `displayMode`'s initial `useState` value, at the user's direct request; the toggle itself and everything else about the list are unchanged
- A toolbar above the list offers a search box (matches name), three filter dropdowns (storage location, category, status) tucked behind a `.filters-toggle` disclosure below 560px (search and the view toggle stay always visible) — a pure-CSS breakpoint split (`display: contents` on the wrapper at desktop, a real collapsible panel only inside the mobile media query), not a JS resize listener, so the desktop row layout is unaffected
- Inline edit mode (inside the modal) exposes the full field set, same validation as the add form

### `App.tsx` — `PageHeading`/`pageTitleFor` — added UI feedback pass two (21/8/26)
- Replaces the old `Breadcrumb` component ("Dashboard › Fridge"-style trail, removed entirely) with a single plain-language title plus one "Home" button. `pageTitleFor(view, place, listStorageFilter, listStatusFilter)` derives the title text (e.g. "Expired in Fridge," "All items," "Needs attention") from the same routing state `App.tsx` already tracks; `PageHeading` renders that title beside a house-icon button that always calls `navigateTo('dashboard')` regardless of how many steps preceded it — one control for "how do I get back" instead of a chain of clickable breadcrumb segments
- Not rendered on the Dashboard itself (`view === 'dashboard'`), only on `place-dashboard`/`list`, since the Dashboard is already "home"

### `index.css` — colour tokens — re-hued UI feedback pass two (21/8/26); status colours reverted UI feedback pass three (21/8/26)
- `--color-primary`/`--color-primary-hover` changed from a flat teal (`#0f8a72`) to Tailwind's indigo-600/700 (light: `#4f46e5`/`#4338ca`) and indigo-500/400 (dark: `#6366f1`/`#818cf8`) — indigo-600 against white text clears the 4.5:1 contrast ratio for normal text, a documented pairing rather than a colour chosen purely by eye. Unaffected by the pass-three revert below — the user's feedback there was specifically about the status colours, not the brand colour
- `--color-fresh-*`/`--color-soon-*`/`--color-expired-*`/`--color-warning-*` were briefly re-hued into a green→amber→orange→red severity ramp (21/8/26), then reverted (UI feedback pass three, same day) back to the exact original green/yellow/red/purple hex values — pulled from the last real git commit (`git show HEAD:src/index.css`) rather than approximated — at the user's direct, explicit preference for the original set over the severity-ramp argument
- `--color-place-*` (Fridge/Freezer/Pantry) and `--color-sidebar-*` are unchanged — already validated/justified in the reference-matched redesign entry above

## Current State (as of 18/8/26)
- **Slice 1 (App Shell) is fully done and verified.** Full sign-up → email confirmation → sign-in → add item → see it in the list works end-to-end against the live Supabase project. Also fixed a real bug: Auth's Site URL was pointed at `localhost:5173` — now points at `https://expiry-mate.vercel.app` (see `decisions.md`, "Auth redirect URL fix"). RLS-split migration run and confirmed.
- **Slice 2 (Full Schema Forms) is code-complete, not yet browser-tested.** `tsc -b --force` and `eslint` both pass clean. The `(user_id, expiry_date)` index migration hasn't been run against the live project yet. Also includes the Leftovers date-field relabelling fix (18/8/26). See `docs/09-iteration-log.md`.
- **Slice 3 (Adjusted Expiry Logic) is fully done and verified.** `src/lib/adjustedExpiry.ts` implements all 11 categories (Eggs newly added — see `decisions.md`, "Category/algorithm reconciliation"); `ItemList` shows the adjusted date, days remaining, and status badge/warning derived from it, memoised per row. Eggs migration run against the live project; browser-tested (Eggs, Frozen, storage/opened toggles all confirmed working).
- **Slice 4 (Styling) is code-complete, not yet browser-tested.** Revised several times based on feedback: minimal two-row item card became a click-to-expand card (countdown/status/name collapsed, everything else in a centred animated modal), a manual dark-mode toggle (`src/hooks/useTheme.ts`, persisted, flash-free via a small inline script in `index.html`), a full-bleed desktop layout (`.item-list` as a responsive grid, sticky full-width header) that still collapses to one column on a phone, the add-item form moved off the dashboard behind a "+" FAB button, a toolbar with search/filter/sort added to `ItemList.tsx` (warnings always pinned to their own top section), every form field (add + edit) now has a visible title, and deleting an item now requires an in-place confirmation step instead of happening immediately. A real gap was also caught and fixed along the way — `App.css` was never actually imported anywhere, so `App.tsx` now imports it. `tsc -b --force` and `eslint` both pass clean.
- **Slice 5 (Expiry Notifications) is code-complete, not yet browser-tested.** `useExpiryNotifications` fires one batched browser `Notification` per page load listing items within 7 days of (or past) their adjusted expiry date, reusing status logic newly extracted into `src/lib/itemStatus.ts`; `ExpiryBanner` shows the same list directly on the dashboard as an always-visible, dismissible fallback since notifications can be denied/blocked/missed. The optional email-digest stretch (Supabase Edge Function) was deliberately not built — needs an external email-provider API key and scheduling setup — and is recorded as a scope decision, not a gap, in `decisions.md`. `tsc -b --force` and `eslint` both pass clean. All Must Have requirements are now code-complete.
- **Slice 6 (Nice-to-Haves) is partially built.** Two of the four items are code-complete: auto-category suggestion from the item name (`src/lib/guessCategory.ts`, a keyword lookup with no AI/network call, hand-verified against 18 cases) and defaulting the add form's date field to today instead of blank. The other two (AI recipe suggestions, multi-user household sharing) were deliberately deferred at the user's request, not dropped — see `decisions.md`, "Slice 6 scope." Alongside these, two real UI bugs reported directly against the live app were found and fixed: cards turning a low-contrast bright colour on hover (a global `button:hover` rule winning the cascade over the card's own background), and the mobile layout not matching desktop (a flex-basis meant for width being reinterpreted as height once the mobile toolbar switches to a column layout). A follow-up request then collapsed the mobile toolbar's 3 filter selects + sort toggle behind a single "Filters" disclosure button, cleaning up a layout that previously showed 4 full-width control rows before a single card. All three were verified with real before/after screenshots via a disposable local Playwright preview (see `decisions.md`, "Card hover contrast + mobile search box sizing") — not just `tsc`/`eslint` passing, the first time this project's UI was actually visually confirmed rather than only compiled. `tsc -b --force` and `eslint` both pass clean.
- **Supabase's GitHub "Deploy to production" integration is now connected and live**, after reconciling the migration-history mismatch found before connecting it (see `decisions.md`, "GitHub deploy integration"). `supabase migration list` confirms the intended state: `20260507000000`, `20260601000000`, `20260818000000`, and `20260818020000` all correctly marked applied, `20260818010000` (the index migration) correctly still pending — the next merge to `main` will apply only that one migration for real. **Outstanding:** the user's database password passed through this chat in plaintext while troubleshooting the repair command; rotating it via Settings → Database has been recommended more than once but not confirmed done.
- **Feedback Sprint opened 21/8/26** — a separate, explicitly-not-a-slice piece of work, in response to direct teacher feedback on the live app (not planned nice-to-haves). Deadline extended to next Friday. Full detail, discussion, and plan of attack: `docs/10-feedback-sprint.md`. **Rough wireframe built, refined, and agreed (21/8/26):** a self-contained, clickable HTML mockup at `docs/wireframes/feedback-sprint-dashboard-wireframe.html`. **Database seed generated and run (21/8/26):** `supabase/seed/feedback-sprint-seed.sql` — 135 items across every category/storage/status, each row's date computed by inverting the real adjusted-expiry algorithm for a chosen target status and then re-verified forward through the same algorithm (caught 4 real generator bugs before the file was finalised — see `decisions.md`); confirmed run by hand in the Supabase SQL Editor. **Real UI built (21/8/26):** `App.tsx`/`ItemList.tsx` rewritten and two new components (`Dashboard`, `PlaceDashboard`) added, matching the wireframe's dashboard-first structure — see the `Key Components` entries above and `decisions.md`, "Feedback Sprint: real UI build," for what changed and why (dropping the old grouped-sections UI, reusing the item modal for table rows, the new "Needs attention" filter, the validated storage-location colour palette). Verified via `tsc`/`eslint`/`npm run build` all passing clean plus a disposable Playwright harness screenshotting the whole flow against a generated dataset, zero console errors. **Seed account mismatch found and fixed (21/8/26):** confirming against the real app surfaced that both seed files had been written against a different account of the user's (`0b3816cf-2946-4363-9490-313fd82f865a`) than the one actually signed in (`609e83a3-ec8e-489d-865a-353e769d5659`) — the insert succeeded but RLS correctly hid the rows. Fixed with a single `update` re-parenting all 155 previously-inserted rows (135 + the original 20-item seed, which had the same problem all along) onto the correct account; both seed files' hardcoded UUID corrected too. See `decisions.md`, "Feedback Sprint seed account mismatch." **Visual polish pass (21/8/26):** Inter font, a radius/shadow/easing token scale, tabular numerals throughout, a real favicon, a header brand mark, and a search-field icon — a finishing pass on top of the just-built structure, not a redesign; see `decisions.md`, "Feedback Sprint: visual polish pass." **Reference-matched redesign (21/8/26):** against a real admin-template screenshot and a literal six-point list from the user, made the stat tiles gapless with one accent tile, gave `Donut.tsx` a real punched centre showing the segment total, replaced the second donut with a new `BarChart.tsx` of the same storage-location data, built a new persistent `Sidebar.tsx` (profile block, nav, theme/sign-out footer, constant dark navy) replacing the old top header, and fixed `ExpiryBanner`'s uncapped name list via a new shared `lib/formatNameList.ts`. See the `Key Components` entries above and `decisions.md`, "Feedback Sprint: reference-matched redesign." Verified via `tsc`/`eslint`/`npm run build` all clean plus a rebuilt Playwright harness screenshotting desktop/tablet/phone widths. **UI feedback pass two (21/8/26):** a further eight-point list against a fresh screenshot — `ExpiryBanner` removed from the render tree entirely (the needs-attention card already covers it), a show/hide password toggle on `Auth.tsx`, the dashboard's visible empty space closed with a deterministic 2-column charts layout (rejected CSS multi-column as browser-unpredictable), a full colour re-hue (indigo primary, a proper green→amber→orange→red status severity ramp fixing the old purple-unsafe ordering bug), `Sidebar.tsx` given a persisted manual collapse/expand, the four dashboard stat tiles made clickable and scoped to whichever dashboard they're on, the breadcrumb trail replaced by a single title plus a "Home" button (`PageHeading`), and the "View all X items" link restyled as a real button. See the `Key Components` entries above and `decisions.md`, "Feedback Sprint: UI feedback pass two," for full reasoning on each — explicitly requested by the user for documentation credit. Verified via `tsc`/`eslint`/`npm run build` all clean plus a rebuilt Playwright harness confirming all eight changes render/behave as intended, zero unexpected console errors. **UI feedback pass three (21/8/26):** the user explicitly encouraged using UI libraries (correcting the tech-stack table's "no UI library" line, which was a project decision, not a user constraint) — added `framer-motion`, scoped to animation. Removed the "Jump to a place" quick-nav row (redundant with the sidebar) and its CSS. Reverted the four status colours to the exact original green/yellow/red/purple values, undoing the previous pass's severity-ramp re-hue at the user's direct request (indigo primary untouched). Reworked the four stat tiles so clicking one expands that status's full list in place — a new shared `AttentionPanel.tsx` used by both `Dashboard` and `PlaceDashboard`, animated with a framer-motion crossfade/height animation and per-tile hover/press motion — replacing the previous pass's "click navigates to a new page" behaviour. See the `Key Components` entries above and `decisions.md`, "Feedback Sprint: UI feedback pass three." Verified via `tsc`/`eslint`/`npm run build` all clean (bundle-size increase from the new dependency noted as an accepted tradeoff) plus a rebuilt Playwright harness confirming the panel expands/swaps/collapses with no page navigation, scoped correctly on a place-dashboard, and rendering correctly in dark mode. **UI feedback pass four (21/8/26):** a six-point follow-up — extracted `ItemList.tsx`'s item-detail modal/state machine into shared `ItemDetailModal.tsx`/`useItemDetail.ts` so `AttentionPanel`'s rows (now real clickable buttons) can open it too, from `Dashboard`/`PlaceDashboard`; capped/centred `.dash-grid` at 1100px (scoped to the dashboard grids only); rebuilt `Donut.tsx` as a real SVG ring (gapped rounded segments, hover-linked ring/legend highlighting) and redesigned `BarChart.tsx` to match (grow-in animation, background rail, hover dim/highlight); fixed the stat-tile hover bug (removed the lift that opened a gap in the flush row and dragged the active ring away, replaced with CSS-only hover feedback); and fixed the active ring not curving on the outer tiles by giving them their own explicit `border-radius`. See the `Key Components` entries above and `decisions.md`, "Feedback Sprint: UI feedback pass four," for full reasoning on all six. Verified via `tsc`/`eslint`/`npm run build` all clean (one real bug caught by `tsc`: an unclosed JSX comment in `AttentionPanel.tsx`) plus a rebuilt Playwright harness at a 1920px viewport confirming every point, including the dashboard grid's centring via its actual computed layout, not just a screenshot. **UI feedback pass five (21/8/26):** a one-line follow-up — `ItemList.tsx`'s `displayMode` now defaults to `'table'` (list view) instead of `'card'`, so list view loads first everywhere the list is reached from; nothing else about the toggle changed. **UI feedback pass six (21/8/26):** a screenshot flagged "too much empty space" on the status-breakdown and items-by-location dashboard cards. Measured the actual rendered layout in a disposable harness before changing anything — the donut card's box height already matched its content exactly, so the real issue was horizontal: `.donut-row` had no `justify-content`, leaving the ring+legend hugged to the card's left edge with everything past them empty. Fixed with `justify-content: space-between; width: 100%` on `.donut-row` (anchoring the legend to the card's right edge, with a column-direction override in the existing mobile breakpoint) plus a bump to the dashboard's donut size (140px → 168px) so the two chart cards' heights line up more closely. See `decisions.md`, "Feedback Sprint: UI feedback pass six." Verified via `tsc`/`npm run build` clean and `eslint src` clean, plus re-measuring the harness's actual computed layout after the fix. **UI feedback pass seven (21/8/26):** a three-point follow-up — fill the gap below the dashboard's charts, make "Needs attention" always fill the screen and scroll internally instead of expanding/collapsing, and make the bar chart look nicer with nicer colours. Built a CSS flexbox/grid stretch chain anchored at a new `.app-main-fill` class (`height: 100vh`, applied only to the two dashboard views in `App.tsx`) so both the charts column and `AttentionPanel` now fill a full viewport height with no leftover gap; confirmed via a Playwright harness that `.app-shell`'s pre-existing `min-height: 100vh` alone wasn't a definite height flexbox/grid could stretch against, which a first attempt relied on and which just let the whole page grow to fit content instead. Restructured `AttentionPanel.tsx` to drop its height-animating `layout` prop and 10-row cap in favour of a fixed-height card whose `.attention-list` scrolls internally, with the "See full list" button pinned at the bottom (`Dashboard.tsx`/`PlaceDashboard.tsx` now pass an uncapped limit to `buildNeedsAttention`). Checked the bundled dataviz skill's reference palette and found the existing storage-location colours already byte-identical to its validated 3-series set, so rather than re-hue them, `BarChart.tsx` now renders each bar as a `color-mix()` gradient with a hover brightness lift instead of a flat fill. See the `Key Components` entries above and `decisions.md`, "Feedback Sprint: UI feedback pass seven." Verified via `tsc`/`eslint`/`npm run build` all clean plus a rebuilt Playwright harness that measured the real computed layout (not just a screenshot) at desktop, tablet, and phone widths — catching one real bug in the process (`.attention-panel-content` using `height: 100%` instead of `flex: 1`, which silently failed to resolve and let the page grow instead of scrolling) before it shipped. **UI feedback pass seven bug fix (21/8/26):** a live screenshot right after delivery showed the four stat tiles' numbers clipped partway down. Cause: `.stat-row`'s pre-existing `overflow: hidden` disables a flex item's usual protection against shrinking below its own content — so `.stat-row` (not `.attention-card`, which has a zero flex-basis) absorbed any shortfall in the now-capped `.dash-grid` by being silently squashed, at every viewport height tested, not just short ones. Fixed with one line, `flex-shrink: 0` on `.stat-row`. Re-verified via the same Playwright harness at six viewport heights (600–900px) on both `Dashboard` and `PlaceDashboard`, in light and dark mode. See `decisions.md`, "Feedback Sprint: UI feedback pass seven" (bug-fix addendum). Next: deliver this batch to the user's repo; the user's own visual check on their machine.
- What's live on Vercel is still the old placeholder until this work gets pushed and Vercel redeploys.
- `tests/{unit,integration,smoke}` exist as folders but are empty — tests get written as each slice's logic lands, not upfront. `adjustedExpiry.ts` is written as a pure function specifically so it's easy to unit test once that starts.
- Repo structure, security floor, and the slice plan itself have all been reconciled against the actual assessment brief (see `docs/decisions.md`) — the plan below reflects that review, including the performance/security additions folded into Slices 1, 2, 3, and 5.

## Requirements Summary
### Must Have
- Track food item name + expiry date
- Add / edit / delete items
- Track quantity, category, storage location, opened status (schema exists, forms don't collect it yet — Slice 2)
- Notify users when items are about to expire (Slice 5 — code-complete, not yet browser-tested)
- User auth + per-user database storage ✅

### Nice to Have
- Auto-category suggestion from item name ✅ (Slice 6, 18/8/26)
- Default the add form's date field to today ✅ (Slice 6, 18/8/26)
- AI-powered recipe suggestions based on expiring ingredients (deferred — see `decisions.md`, "Slice 6 scope")
- Multi-user household sharing / shared pantry (deferred — see `decisions.md`, "Slice 6 scope")

### Out of Scope
- Barcode / QR code scanning
- Nutritional information
- Image recognition

Full detail: `docs/02-requirements.md`.

## Development Commands
```bash
npm run dev       # start Vite dev server
npm run build     # TypeScript check + Vite build
npm run lint      # ESLint
npm run preview   # preview production build
```

## Next Logical Steps
1. ~~Slice 1 — App Shell~~ Fully done and verified 18/8/26, including the RLS-split migration.
2. Slice 2 — Full Schema Forms: code-complete 18/8/26, plus the Leftovers relabelling fix. Still need to browser-test and run the `(user_id, expiry_date)` index migration.
3. ~~Slice 3 — Adjusted Expiry Logic~~ Fully done and verified 18/8/26, including the Eggs migration and browser test.
4. Slice 4 — Styling: code-complete 18/8/26 (`index.css` + `App.css`, plus the missing `App.css` import fix, sort/filter/add-button, and delete confirmation). Still need to browser-test.
5. Slice 5 — Expiry Notifications: code-complete 18/8/26. The single-batched-notification behaviour is framed explicitly as a performance/UX decision in the report; the optional email-digest stretch was deliberately scoped out (see `decisions.md`). Still need to browser-test.
6. Slice 6 — Nice-to-Haves: auto-category suggestion and default-date-to-today are code-complete 18/8/26 (see `decisions.md`, "Slice 6 scope"). AI recipe suggestions and multi-user household sharing are deferred at the user's request — revisit if time allows once the report and tests are further along. Two real UI bugs (card hover contrast, mobile layout not matching desktop) and a mobile toolbar cleanup (filters collapsed behind a disclosure button) were found/requested and fixed alongside this, visually verified via a disposable Playwright preview rather than only compiled/linted — see `decisions.md`.
7. Supabase's GitHub deploy integration is connected and live 18/8/26 — the migration-history reconciliation is done, `migration list` confirms the correct state. Still outstanding: rotate the database password that passed through this chat during troubleshooting (Settings → Database).
8. Write the Part B report and prep the walk-through — these are separate from the slices and don't happen automatically just by finishing them.
9. `tests/{unit,integration,smoke}` are still empty — worth starting now that all planned Must Have + partial Nice-to-Have code is in, especially unit tests for the pure functions (`adjustedExpiry.ts`, `itemStatus.ts`, `guessCategory.ts`, `validateItem.ts`) since none of them need a browser or Supabase to test.
10. The live app has still only been visually verified via a disposable fake-data preview, never the actual Supabase-connected app in a real browser on a real device — worth doing once there's a moment, especially the mobile filters panel and the card hover fix on an actual phone.
11. **Feedback Sprint (opened 21/8/26, deadline now next Friday):** direct teacher feedback — dashboard-as-landing-page with a summary/warnings, intuitive navigation to specific places (e.g. the fridge, what's expiring), a card-view/list-view toggle, and a concern that the current card+filter layout won't scale to a realistic item count. Full plan in `docs/10-feedback-sprint.md`. Wireframe built, refined, and agreed 21/8/26 (`docs/wireframes/feedback-sprint-dashboard-wireframe.html`). Database seed generated and run 21/8/26 (`supabase/seed/feedback-sprint-seed.sql`, 135 items). Real UI built 21/8/26 — `Dashboard`/`PlaceDashboard`/rewritten `ItemList` — verified via `tsc`/`eslint`/build plus a disposable Playwright harness against generated mock data. **Seed account mismatch found and fixed 21/8/26** — both seed files had targeted the wrong one of the user's accounts, so RLS was correctly hiding all 155 seeded rows; fixed with an `update` re-parenting them onto the right account (see `decisions.md`). **Visual polish pass 21/8/26** — Inter font, a radius/shadow/easing token scale, tabular numerals throughout, a real favicon, a header brand mark, and a search-field icon, on top of (not replacing) the just-built structure; see `decisions.md`, "Feedback Sprint: visual polish pass." **Reference-matched redesign 21/8/26** — the user supplied a real admin-template screenshot and a literal six-point list: gapless stat tiles (now one card with dividers, one accent tile), a real punched centre on the status donut showing its total, the second donut replaced by a new `BarChart.tsx` of the same storage-location counts, a new persistent `Sidebar.tsx` (profile block with an email-derived name, five nav destinations, theme/sign-out footer, constant dark navy regardless of theme) replacing the old top header, and `ExpiryBanner`'s previously-uncapped name list now sharing `useExpiryNotifications`'s truncation rule via a new `lib/formatNameList.ts`. See `decisions.md`, "Feedback Sprint: reference-matched redesign," including what was deliberately left out (no calendar widget, no area chart, no second CTA button). **UI feedback pass two 21/8/26** — a further eight-point list against a fresh screenshot, closing with an explicit request to document every decision for extra marks: `ExpiryBanner` removed from the render tree (superseded by the Dashboard's own needs-attention card, rather than shrunk again); a show/hide password toggle on `Auth.tsx`; the dashboard's visible empty space closed with a deterministic 2-column charts layout instead of CSS multi-column; a full colour re-hue (accessible indigo primary, plus a genuine green→amber→orange→red status severity ramp fixing a pre-existing bug where "unsafe" was purple); `Sidebar.tsx` given a persisted manual collapse/expand; the four dashboard stat tiles made real clickable buttons, scoped to "all items" or "this place's items" depending on which dashboard they're clicked from; the breadcrumb trail replaced by one plain-language title plus a "Home" button (`PageHeading`/`pageTitleFor`); and "View all X items" restyled as a proper bordered button. Full reasoning for each in `decisions.md`, "Feedback Sprint: UI feedback pass two." Verified via `tsc`/`eslint`/`npm run build` all clean plus a rebuilt Playwright harness confirming all eight render/behave as intended (including that clicking a stat tile from a place-dashboard correctly scopes to just that place), zero unexpected console errors. **UI feedback pass three 21/8/26** — a four-point follow-up: the user explicitly encouraged using UI libraries (the tech-stack table's "no UI library" line was a project decision, not a user constraint — corrected), so `framer-motion` was added, scoped to animation; the "Jump to a place" quick-nav row was removed (redundant with the sidebar); the four status colours were reverted to the exact original green/yellow/red/purple values, undoing the previous pass's severity-ramp re-hue at the user's direct request; and the four stat tiles were reworked into a new shared `AttentionPanel.tsx` (used by both `Dashboard` and `PlaceDashboard`) that expands a status's full list in place with a framer-motion crossfade/height animation, rather than navigating to a new page. Full reasoning in `decisions.md`, "Feedback Sprint: UI feedback pass three." Verified via `tsc`/`eslint`/`npm run build` all clean plus a rebuilt Playwright harness confirming the panel expands/swaps/collapses correctly with no page navigation and scopes correctly on a place-dashboard. **UI feedback pass four 21/8/26** — a six-point follow-up: every list's rows are now clickable to the same expanded-item view (shared `ItemDetailModal.tsx`/`useItemDetail.ts`, extracted from `ItemList.tsx`); the dashboard grid is capped and centred at wide viewports; `Donut.tsx` rebuilt as a real SVG ring and `BarChart.tsx` redesigned to match, both with mount animation and hover interactivity; the flush stat-tile hover bug (lift revealing a gap, dragging the active ring loose) fixed by moving hover feedback into CSS only; and the active ring now genuinely curves on the first/last tile via an explicit `border-radius` rather than only looking rounded through the row's own clipping. Full reasoning in `decisions.md`, "Feedback Sprint: UI feedback pass four." Verified via `tsc`/`eslint`/`npm run build` all clean plus a rebuilt Playwright harness at a 1920px viewport confirming every point, including the grid's centring via its actual computed layout. **UI feedback pass five 21/8/26** — `ItemList.tsx`'s `displayMode` now defaults to `'table'` instead of `'card'`, so list view loads first everywhere the list is reached from. **UI feedback pass six 21/8/26** — fixed a reported "too much empty space" on the status-breakdown/items-by-location cards: `.donut-row` gained `justify-content: space-between` so its legend anchors the card's right edge instead of hugging the ring with a large blank block beyond it, and the dashboard's donut size was bumped from 140px to 168px so the two chart cards' heights line up more closely. Diagnosed and confirmed via a disposable harness's actual computed layout, not just a screenshot — see `decisions.md`, "Feedback Sprint: UI feedback pass six." **UI feedback pass seven 21/8/26** — a three-point follow-up: filled the gap below the dashboard's charts and made "Needs attention" always fill the screen and scroll internally instead of expanding/collapsing, via a CSS flexbox/grid stretch chain anchored at a new `.app-main-fill` class (`height: 100vh`, applied only to the two dashboard views); `AttentionPanel.tsx` dropped its height-animating `layout` prop and 10-row cap in favour of a fixed-height card with an internally-scrolling `.attention-list` and a pinned "See full list" button; and `BarChart.tsx` got a `color-mix()` gradient fill plus hover brightness instead of a flat fill, keeping the existing storage-location colours unchanged since they're already the dataviz skill's validated 3-series palette. Confirmed via a Playwright harness measuring the real computed layout (not just a screenshot) at desktop/tablet/phone widths — this caught a real bug (`.attention-panel-content` needing `flex: 1`, not `height: 100%`) before it shipped. Full reasoning in `decisions.md`, "Feedback Sprint: UI feedback pass seven." **Pass seven bug fix 21/8/26** — a live screenshot caught the stat tiles' numbers being clipped: `.stat-row`'s pre-existing `overflow: hidden` disabled its usual protection against shrinking below its own content, so it silently absorbed any shortfall in the now height-capped `.dash-grid` instead of the flexible `.attention-card`, at every viewport height tested. Fixed with `flex-shrink: 0` on `.stat-row`; re-verified via the same harness on both dashboards, both themes. Still outstanding: deliver this batch to the user's repo; the user's own visual check on their machine.
