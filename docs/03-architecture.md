# Data Dictionary

This is the assessment's data dictionary: every field a user or the system stores, with its display format, size and validation rules. `04-data-model.md` covers the same table from the implementation side (SQL types, enums, constraints, indexes, RLS and migration history). Where the two disagree, `04-data-model.md` is the one checked against the live schema.

## Items Table

| Variable | Data Type | Format for Display | Size (Bytes) | Size for Display | Description | Example | Validation |
|---|---|---|---|---|---|---|---|
| id | UUID | — | 16 | 36 chars | Unique identifier for each item, auto-generated | 3f2a1c… | Must be unique; generated by the database |
| user_id | UUID | — | 16 | 36 chars | Foreign key linking the item to its owner; references the Users table | 9b4e2d… | Must match an existing user id |
| name | String | XX…XX | Up to 255 | Up to 255 chars | Name of the food item | Milk | Required; any non-empty string |
| category | Enum | XX…XX | Up to 50 | Up to 50 chars | Food category used by the algorithm to calculate the adjusted expiry date | Dairy | Required; must be one of: Dairy, Eggs, Meat, Seafood, Produce, Bakery, Frozen, Microwave Meals, Beverages, Condiments, Snacks, Leftovers |
| storage_location | Enum | XX…XX | Up to 10 | Up to 10 chars | Where the item is currently stored | Fridge | Required; must be Fridge, Freezer, or Pantry |
| expiry_date | Date | DD/MM/YYYY | 4 | 10 chars | The printed expiry date as shown on the item's packaging; for Leftovers, the date the meal was prepared, and the field is relabelled "Date prepared" in the form | 23/08/2026 | Required; must be a valid calendar date. For Leftovers it must be on or before today, since a meal cannot be prepared in the future |
| quantity | Integer | NN | 4 | Up to 3 chars | Number of units of the item the user has | 3 | Required; integer between 1 and 999 |
| is_opened | Boolean | Yes / No | 1 | 3 chars | Whether the item has been opened | Yes | Required; defaults to No |
| date_opened | Date | DD/MM/YYYY | 4 | 10 chars | The date the item was opened; used by the algorithm when calculating the adjusted expiry date for opened items | 10/06/2026 | Required if is_opened is Yes; must be a valid date on or before today in the user's own timezone. The database check allows one day beyond the server's UTC date, so a user ahead of UTC can record their own today |
| created_at | Timestamp | DD/MM/YYYY HH:MM | 8 | 16 chars | Date and time the record was created | 07/05/2026 09:00 | Auto-generated by the database |

## Users Table

| Variable | Data Type | Format for Display | Size (Bytes) | Size for Display | Description | Example | Validation |
|---|---|---|---|---|---|---|---|
| id | UUID | — | 16 | 36 chars | Unique identifier for each user, auto-generated | 9b4e2d… | Must be unique; generated by the database |
| email | String | XXX@XXX.XX | Up to 255 | Up to 255 chars | Email address used to log in | user@example.com | Required; must be a valid email format |
| password | String (hashed) | — | 60 | Not displayed | User's password, stored as a secure hash — never in plain text | — | Required; minimum 6 characters |
