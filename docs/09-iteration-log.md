# Iteration Log

Chronological record of testing feedback, deployment events, and what changed as a result. Unlike `decisions.md` (which records *what was decided and why*) and `ai-use-log.md` (which records *AI interactions*), this file records *what happened when the app met real use or a real deploy* — UAT sessions, bug reports, and iteration off the back of them.

## Format

Each entry: date, what happened (test session / deploy / feedback received), what it revealed, what changed (or will change) as a result.

---

## 18/8/26

Repository audit against the assessment's mandated structure (this session). No app-usage iteration yet — this entry exists to mark the point the repo structure was brought into line with `docs/`, `supabase/functions/`, and `tests/{unit,integration,smoke}/` as required, and to note that an existing Vercel deployment is in place and needs to be kept in sync as Slices 1–5 land. Future entries here should be actual UAT/deploy feedback, not structural housekeeping — that belongs in `decisions.md`.

## 18/8/26 (Slice 1 — App Shell)

`App.tsx` rewritten to wire `Auth`, `AddItemForm`, and `ItemList` together with real Supabase session management (`getSession` on mount, `onAuthStateChange` subscription, Sign Out button). Migration `20260818000000_split_items_rls_policies.sql` written to replace the single broad RLS policy with four per-operation ones. Verified `tsc -b --force` compiles clean with no type errors. **Not yet verified (at time of writing):** the actual sign-in/sign-out/add-item flow in a real browser, and the RLS migration hadn't been run against the live Supabase project yet.

## 18/8/26 (Slice 1 — verified end-to-end)

Ran `npm run dev` and tested the real flow. Hit three separate issues along the way, each diagnosed and fixed rather than worked around:

1. **Sign-up threw "Failed to fetch."** Checked the Supabase project dashboard — status showed "Unhealthy," which looked alarming, but the breakdown showed Database/PostgREST/Auth all Healthy; only Edge Functions was unhealthy, and the app doesn't use Edge Functions. Turned out to be transient/environmental, not a real project issue — retried and the request went through.
2. **Sign-up "succeeded" but no confirmation email arrived.** Checked Authentication → Users in the Supabase dashboard and found the email used was an already-registered, already-confirmed account from May — Supabase silently no-ops re-sending confirmation for an existing confirmed email (by design, to avoid leaking account existence), so there was nothing wrong, just a stale test email being reused. Fixed by testing with a fresh email via `+` addressing.
3. **Confirmation link opened on phone redirected to `localhost:5173`, which doesn't load on a phone.** This one was a genuine misconfiguration, not just a testing artifact: Supabase's Auth "Site URL" was set to `localhost:5173`, meaning the exact same broken redirect would happen to a real user signing up on the deployed Vercel app. Fixed in Supabase dashboard → Authentication → URL Configuration: Site URL set to `https://expiry-mate.vercel.app`, with `http://localhost:5173/**` added as an additional redirect URL so local dev still works.

After the URL fix: signed up, confirmed via email, signed in, added a food item, saw it appear in the list. **Slice 1 is genuinely done** — not just "compiles," the whole auth + CRUD loop works end-to-end against the live Supabase project.

## 18/8/26 (Slice 1 — RLS migration run)

Ran `20260818000000_split_items_rls_policies.sql` in the Supabase SQL Editor. Confirmed in the dashboard's policy list: `items` now shows four separate policies (view/insert/update/delete) instead of the original single "Users can manage their own items." **Slice 1 is fully closed out** — code, live verification, and the security enhancement are all done.

## 18/8/26 (Slice 2 — Full Schema Forms, code complete)

`AddItemForm` now collects category, storage location, quantity, opened status, and date opened alongside name and expiry date; `ItemList`'s inline edit exposes the same full set. Both now go through `useItems` and `validateItemForm` instead of talking to Supabase directly. `tsc -b --force` and `eslint` both pass clean. **Not yet verified:** the actual add/edit flow with the full field set hasn't been tested in a real browser, and the `(user_id, expiry_date)` index migration (`20260818010000_add_items_index.sql`) hasn't been run against the live project yet.

## 18/8/26 (Leftovers date field relabelling)

Raised while reviewing Slice 2: for a Leftovers item there's no printed expiry date to read off a label, since it's homemade food. Fixed `AddItemForm.tsx` and `ItemList.tsx` to relabel the date field to "Date prepared" (with a `max` of today) when category is Leftovers, and to display "prepared {date}" instead of "expires {date}" in the item row. `tsc -b --force` and `eslint` both pass clean. **Known, tracked limitation — not a bug:** the status badge (`getStatus`) still evaluates the date as if it were a future expiry date, so a freshly-added Leftovers item will show "Expiring soon" or "Expired" until Slice 3 replaces `getStatus` with `getAdjustedExpiry`. **Not yet verified:** the relabelling hasn't been exercised in a real browser — need to confirm "Date prepared" actually appears for Leftovers in both the add form and inline edit.

## 18/8/26 (Slice 3 — Adjusted Expiry Logic, code complete)

`src/lib/adjustedExpiry.ts` implements `getAdjustedExpiry` for all 11 categories (Eggs newly added, see `decisions.md` "Category/algorithm reconciliation"), plus `getDaysRemaining`/`getExpiryStatus`. `ItemList.tsx` refactored to extract a memoised `ItemRow` component per the marking-alignment review's performance note, replacing the old printed-date-only `getStatus` with the adjusted-date logic; rows now show the adjusted date, days remaining, and either the Fresh/Soon/Expired badge or a warning message for unsafe/not-recommended combinations. Migration `20260818020000_add_eggs_category.sql` adds Eggs to the DB enum. `tsc -b --force` and `eslint` both pass clean; also hand-verified 11 representative cases (one per category, covering unsafe paths and an exact day-0 boundary) via a throwaway script — all matched expected output. **Not yet verified:** the actual browser flow (picking Eggs/Frozen, toggling storage/opened, watching the badge and warning text update) hasn't been tested live yet.

## 18/8/26 (Eggs migration run)

Ran `20260818020000_add_eggs_category.sql` in the Supabase SQL Editor — confirmed working. `Eggs` is now a real, selectable category on the live project. The browser test of the actual add/edit flow (Eggs, Frozen, storage/opened toggles) is still outstanding.

## 18/8/26 (Slice 3 browser test)

Confirmed working: Eggs and Frozen categories, storage/opened toggles, adjusted date + badge/warning text all update correctly in a real browser. Slice 3 is fully closed out — code, migration, and live verification all done.

## 18/8/26 (Slice 4 — Styling, code complete)

`src/index.css` and `src/App.css` written per the plan (reset/typography/variables in the former, layout in the latter). Found and fixed a real gap: `App.css` was never imported anywhere, so the new styles would have done nothing without adding `import './App.css'` to `App.tsx`. `tsc -b --force` and `eslint` both pass clean. **Not yet verified:** hasn't been opened in a real browser — need to check the auth screen, the item list/cards, the status badge colours (including the new warning colour), and the mobile breakpoint (560px) actually render as intended.

## 18/8/26 (Slice 4 revision — minimal look, dark mode, full-width desktop)

Feedback on the first pass: cluttered UI, wanted a nicer palette with dark mode, wanted the layout to use the full desktop screen while still fitting a phone. Reworked the item card into a cleaner two-row layout, added `useTheme.ts` (manual light/dark toggle, persisted, avoids a flash-of-wrong-theme via a small inline script in `index.html`), and changed `.app-header` to a full-bleed sticky bar with `.item-list` as a responsive grid (multiple columns on wide screens, one column on narrow). `tsc -b --force` and `eslint` both pass clean. **Not yet verified:** hasn't been opened in a real browser — need to check both themes, the toggle persisting across a reload, the grid at desktop width, and the mobile layout at a genuinely narrow width.

## 18/8/26 (Slice 4 second revision — meta grid)

Screenshot showed the middle-dot meta line wrapping with a stray leading dot on the second line. Replaced it with a small label/value grid (one row per fact) instead of a wrapped inline sentence; the warning message gets its own full-width row. `tsc -b --force` and `eslint` both pass clean. Still needs a real browser check.

## 18/8/26 (Slice 4 third revision — card polish)

General "make each card look better" pass: rounded status accent bar (via `::before` + `overflow:hidden` instead of a plain border), icon-only circular Edit/Delete buttons matching the theme toggle's style, a small dot on the status badge, uppercase micro-labels in the meta grid, and a subtle hover lift. `tsc -b --force` and `eslint` both pass clean. **Genuinely not yet verified visually** — this environment can't render the app in a browser, so this is a design judgement call, not something checked before delivery. A real screenshot is the next step.

## 18/8/26 (Slice 4 fourth revision — click-to-expand cards)

Bigger restructure: cards collapsed to countdown/status/name only, clicking one opens a centred modal (via `createPortal`) with a "grows from where you clicked" transform animation, every other fact as a stat tile, and Edit/Delete moved into the modal. `tsc -b --force` and `eslint` both pass clean. **Not yet verified in a real browser** — this is the most interaction-heavy change so far (click targets, animated transform, portal, focus/scroll handling), so it's the most likely of the Slice 4 work to need a real fix-up round once actually clicked through, including on a phone-sized viewport.

## 18/8/26 (Slice 4 fifth revision — add button, sort/filter, labelled edit fields)

Add-item form moved off the dashboard behind a "+" FAB button (`App.tsx`), opening in the same shared `AnimatedModal` now used for both the add and detail flows (extracted into `useAnimatedModal.ts`/`AnimatedModal.tsx` so the animation logic isn't duplicated). `ItemList.tsx` gained a toolbar (search + storage/category/status filters + a place-vs-status sort toggle) and grouped-section rendering with warnings always carved out into their own top section regardless of sort mode, each section headed by a small inline SVG icon. Every edit-form field now has a visible title matching the existing date field's style, fixing an invalid nested-`<label>` bug along the way. `tsc -b --force` and `eslint` both pass clean. **Not yet verified in a real browser** — the add button/modal, sort toggle, each filter, search, warnings-on-top, and the new field labels all still need a real click-through, including a phone-sized check now that the toolbar has five controls.

## 18/8/26 (Slice 4 sixth revision — delete confirmation)

Delete no longer happens on a single click — the Delete icon now arms an in-place confirmation step inside the item-detail modal (warning icon, name, Cancel/Delete) rather than opening a native browser confirm or a second modal. Also fixed a real gap: a failed delete used to fail silently; now the error is surfaced under the confirmation. `tsc -b --force` and `eslint` both pass clean. **Not yet verified in a real browser.**

## 18/8/26 (Slice 5 — Expiry Notifications)

New `useExpiryNotifications` hook fires one batched browser `Notification` per page load listing items within 7 days of (or past) their adjusted expiry date, reusing status logic newly extracted into `src/lib/itemStatus.ts`. A new `ExpiryBanner` shows the same list directly on the dashboard as a dismissible, always-visible fallback, since notifications can be denied/blocked/missed. The optional email-digest stretch (Supabase Edge Function) was deliberately not built — needs an external email-provider API key and scheduling setup outside what's available before Monday; recorded as a scope decision in `decisions.md`, not a gap. `tsc -b --force` and `eslint` both pass clean. **Not yet verified in a real browser** — the permission prompt, the notification firing, and the banner all still need a real check. Slice 5 (Must Have requirements) is now code-complete.

## 18/8/26 (Slice 6 — auto-category suggestion + default date to today)

Built the two quick-win nice-to-haves already planned in `decisions.md`: `AddItemForm` now guesses the category from the item name as it's typed (`src/lib/guessCategory.ts`, a keyword lookup with no AI/network call), stopping as soon as the user picks a category themselves, and the add form's date field now defaults to today instead of blank. AI recipe suggestions and multi-user household sharing (the other two Slice 6 items) were deliberately deferred, not dropped — the user asked to revisit them later. `tsc -b --force` and `eslint` both pass clean; `guessCategory` was also hand-verified against 18 cases via a throwaway script before wiring it in. **Not yet verified in a real browser.**

## 18/8/26 (Slice 4 seventh revision — food-category icons on cards)

Added a hand-drawn icon per food category, shown beside the item name on the collapsed card, in the modal heading, and next to the "Category" stat value — category had never been visible on the card itself before this. No icon library, same plain-SVG style as the rest of the app. `tsc -b --force` and `eslint` both pass clean. **Not yet verified in a real browser** — whether each icon actually reads clearly at card size is a genuinely open question until it's actually seen.

## 18/8/26 (Supabase GitHub deploy integration — caught before connecting)

While helping set up Supabase's GitHub deploy integration, found that every migration so far was applied by hand in the SQL Editor rather than through Supabase's own tooling — meaning the integration would believe none of the 5 migrations had run and would try to replay all of them from scratch on the next merge, failing immediately since the early ones aren't idempotent. Also found `seed.sql` sitting inside `supabase/migrations/`, where it risked being replayed against production as if it were a real migration. Moved `seed.sql` to `supabase/seed/` and prepared `supabase migration repair` commands for the 4 already-applied migrations (deliberately excluding the still-outstanding index migration) for the user to run locally. Caught before the integration was actually connected, not after a broken deploy.

## 18/8/26 (Migration repair completed, GitHub integration connected)

The user ran the prepared repair command. Worked through two real blockers along the way — a missing `SUPABASE_DB_PASSWORD` (fixed via `read -s` so it wouldn't land in shell history), then a genuine network-level timeout to the pooler traced to the network the user was on at the time (confirmed with a raw port check, resolved by switching networks). `migration list` afterwards showed exactly the intended state: 4 migrations correctly marked applied, the index migration correctly still pending. The GitHub "Deploy to production" integration is now connected and live. **Outstanding:** the user's database password passed through this chat in plaintext during troubleshooting; rotating it via Settings → Database has been recommended more than once but not confirmed done.

## 18/8/26 (Card hover contrast + mobile search box sizing — fixed and visually verified)

Two reported bugs: cards turning a bright, low-contrast colour on hover, and the mobile layout not matching the desktop one. Root-caused both by reading the actual CSS cascade rather than guessing — a global `button:hover` rule overriding the card's own background on hover, and a flex-basis meant for width getting reinterpreted as height once the mobile toolbar switches to a column layout. Fixed both with small `App.css` additions. **First real methodology change:** rather than shipping on `tsc`/`eslint` passing alone, built a disposable local Vite + Playwright preview (never committed) rendering the actual components against fake data, and used it to screenshot both bugs reproduced, then both fixes confirmed, at desktop and phone widths, in both themes, before pushing. Desktop confirmed pixel-identical before/after.

## 18/8/26 (Mobile filters collapsed behind a disclosure button)

The three filter selects and the sort-mode toggle now collapse behind a single "Filters" button on phone-width screens (search stays always visible), fixing a layout that previously showed four full-width control rows before a single item card was visible. Implemented as a pure-CSS breakpoint split (`display: contents` on desktop, a real collapsible panel only inside the existing mobile media query) so no JS resize-awareness was needed. Verified with the same disposable screenshot preview: closed, open, and with an active-filter badge, in both themes; desktop confirmed unchanged.

## 21/8/26 (Feedback Sprint opened — teacher UX review)

Teacher reviewed the live app and gave direct feedback: the card/filter list won't scale to a realistic item count (~100), the landing page should be a dashboard (summary + warnings) rather than the full item list, navigation should let the user jump to specific places (e.g. the fridge, what's expiring), and there should be a card-view/list-view toggle. Due date extended to next Friday at the same time. Opened as a separate "Feedback Sprint" (not "Sprint 7") to keep it distinct from the planned six-slice structure. Full discussion, decisions, and plan of attack are in the new `docs/10-feedback-sprint.md`. Nothing built yet — this entry marks the sprint's start, not a shipped change.

## 21/8/26 (Feedback Sprint wireframe built, reviewed, and refined)

Built the agreed rough wireframe — a self-contained, clickable HTML/CSS/JS mockup with a stable 112-item mock dataset — covering the dashboard, Fridge/Freezer/Pantry navigation, and a card/list toggle with matching search/filter/sort. First pass was reviewed and agreed as a good structure, then refined per four follow-up requests: items now default-sort everywhere by how close they are to expiring; clicking a place tile opens a smaller scoped mini-dashboard instead of the full list, with a new 3-level breadcrumb; the mobile list view now shows only name/days-left/status by default with a tap-to-expand row for the rest; and the wireframe is now committed into the repo at `docs/wireframes/feedback-sprint-dashboard-wireframe.html`. Verified throughout with a disposable Playwright screenshot harness (desktop + phone, multiple nav paths, checked programmatically for sort order and breadcrumb text, zero console errors). One real bug caught and fixed along the way: the table's desktop min-width was still forcing the mobile table wider than the phone screen, pushing the Status column off-screen. Status checklist in `docs/10-feedback-sprint.md` updated accordingly.

## 21/8/26 (Feedback Sprint database seed generated — not yet run)

Wireframe marked reviewed and agreed; moved to the plan of attack's next step, seeding the live database. Generated a 135-item seed (`supabase/seed/feedback-sprint-seed.sql`) covering every category, storage location, and status — including deliberately unsafe combinations — by inverting the real Adjusted Expiry Date Algorithm per row and then independently re-checking every row forward through the same algorithm before writing any SQL. That two-way check caught four real generator bugs (a couple of rules picking a `date_opened` that could land in the future, a sign error in the verification logic itself, and Leftovers rows incorrectly marked "opened") before the file was finalised at zero mismatches. Seeded against the same real account as the existing Slice-1-era seed, which is left in place rather than replaced. Still needs to be run by hand in the Supabase SQL Editor, same as every migration on this project — not yet run.

## 21/8/26 (Feedback Sprint seed run; real UI built)

Seed confirmed run against the live database. Built the real UI against the wireframe's agreed structure: a `Dashboard` landing page (stat tiles, status/storage donuts, needs-attention list, quick-nav to each place), a `PlaceDashboard` per storage location, and `ItemList` rewritten to drop its old grouped-sections/sort-mode UI in favour of one flat urgency-sorted list with a card/table view toggle — table rows open the same modal cards already use rather than a second expand-row mechanism. `App.tsx` now routes between the three views with a breadcrumb back to the Dashboard. Added a new, dataviz-skill-validated categorical palette for the storage-location donut; the app's existing status colours stay as-is (reserved, already labelled, not swappable) despite the same validator flagging them for a different use case. Full reasoning in `docs/decisions.md`, "Feedback Sprint: real UI build." Built and verified entirely in a disposable sandbox (`tsc`, `eslint`, `npm run build` all clean) plus a disposable Playwright harness screenshotting the whole flow — dashboard, place-dashboards, both list views, all breadcrumb depths, and the full mobile flow — against a ~120-item generated dataset, zero console errors. One real bug caught and fixed: a status pill's `white-space: nowrap` fought the mobile table's column width, pushing "Expiring soon" off the edge instead of wrapping. Still to confirm: the real Supabase-connected app against the actual live 135-item dataset, not just the harness's mock data.

## 21/8/26 (Feedback Sprint seed account mismatch found and fixed)

Confirming against the live app surfaced a real bug: none of the seeded items appeared. Both seed files had been written against `0b3816cf-2946-4363-9490-313fd82f865a`, a different account of the user's than the one actually signed into the app (`609e83a3-ec8e-489d-865a-353e769d5659`) — the insert genuinely succeeded, but RLS correctly hid every row from the account being used to look at it, which had evidently also been true of the original 20-item Slice 1 seed all along. Diagnosed with two read-only checks (`select count(*) from items;`, `select id, email from auth.users;`) before proposing a fix, rather than guessing. Fixed with a single `update` re-parenting all 155 previously-inserted rows onto the correct account, rather than re-running either seed and duplicating the data; also corrected the hardcoded UUID in both seed files for accuracy if either is run again, with a note documenting the original mismatch. Not yet confirmed: that the items now actually show up in the app.

## 21/8/26 (Visual polish pass)

The user asked about a skill/command for "the best UI" mirroring real-world sites; when offered a choice of mechanisms (named reference, live browsing, a reusable design-system skill) they instead said plainly they just want the current app to look better. Did a direct visual-quality pass rather than more discussion: Inter font (Google Fonts, system stack kept as fallback), a 3-step radius scale and 2-tier shadow scale replacing scattered one-off values, a shared easing token across hovers/toggles, `tabular-nums` everywhere a number appears, a press-down affordance on plain buttons, a real favicon (referenced in `index.html` but never actually created), a header brand mark reusing the Produce category icon's leaf glyph, and a search icon inset into the item search field. Scoped deliberately as polish on the just-built structure, not a second redesign — see `docs/decisions.md`, "Feedback Sprint: visual polish pass." Verified in the same disposable sandbox (`tsc`, `eslint`, `npm run build` all clean) plus a rebuilt Playwright harness re-screenshotting the dashboard (light + dark), both list views, the modal, and mobile. Still to confirm: whether this actually reads as "the best UI" to the user, or whether a concrete named reference is needed next.

## 21/8/26 (Feedback Sprint: reference-matched redesign)

The user supplied a real admin-template reference and a literal six-point list. Made the stat tiles gapless (one card with hairline dividers instead of four separately-carded tiles) with an accent-coloured first tile; gave `Donut.tsx` a real punched centre showing the segment total; replaced the dashboard's second donut (storage-location breakdown) with a new `BarChart.tsx` plotting the same counts, since comparing a magnitude across 3 categories is a bar chart's job per the dataviz skill, not a donut's; built a new persistent `Sidebar.tsx` (profile block with an email-derived name/initials, five nav destinations with active-state highlighting, theme toggle + sign-out footer) replacing the old top header, kept a constant dark navy regardless of the light/dark toggle; and fixed `ExpiryBanner`'s "too crowded and huge" problem by sharing `useExpiryNotifications.ts`'s existing 5-name-plus-"and N more" cap via a new `lib/formatNameList.ts` instead of leaving the banner's name list uncapped. Full reasoning in `docs/decisions.md`, "Feedback Sprint: reference-matched redesign." Verified via `tsc`/`eslint`/`npm run build` all clean plus a rebuilt Playwright harness (with a `Sidebar`-aware navigation stand-in) screenshotting the dashboard at desktop/tablet/phone widths, a place-dashboard, and the all-items view — confirmed the flush stat tiles, the donut's real hole, the bar chart, the sidebar's active-state highlighting, and the banner staying a short capped line even with 29 mock items due. Still to confirm: the user's own reaction against the reference they supplied.

## 21/8/26 (Feedback Sprint: UI feedback pass two)

A further eight-point list against a fresh screenshot of the running app. Removed `ExpiryBanner` entirely rather than shrinking it again — the Dashboard's own needs-attention card already covers the same ground. Added a show/hide password toggle to `Auth.tsx`. Closed the dashboard's visible empty space with a deterministic two-column layout (the two shorter chart cards stacked beside the taller needs-attention card) rather than CSS multi-column, which was rejected as too browser-unpredictable with only 3 cards. Re-hued the primary colour to an accessible indigo and re-hued all four status colours into a proper green→amber→orange→red severity ramp, grounded in the dataviz skill's status-role palette — fixing a real pre-existing bug where "unsafe" was purple, breaking the severity ordering. Added sidebar collapse/expand (icons-only collapsed, persisted like the theme toggle), catching and fixing one real CSS specificity conflict against the existing mobile breakpoint along the way. Made the four dashboard stat tiles real clickable buttons, scoped to "all items" or "this place's items" depending on which dashboard they're clicked from — `App.tsx` decides the scope in one place. Replaced the "Dashboard › Fridge" breadcrumb trail with a single plain-language title plus one "Home" button. Restyled "View all X items →" as a proper bordered button. Full reasoning for each of the eight in `docs/decisions.md`, "Feedback Sprint: UI feedback pass two" — explicitly requested by the user for documentation credit. Verified via `tsc`/`eslint`/`npm run build` all clean plus a rebuilt Playwright harness (extended to screenshot the password toggle and to replicate `App.tsx`'s new Home-button heading, which isn't exported from `App.tsx` itself) confirming the banner is gone, the new palette renders in both themes, clicking a stat tile navigates correctly both globally and scoped to a place, the sidebar collapses to icons, and the password field's visibility toggles — zero unexpected console errors. Still to confirm: the user's own visual check on their machine, and delivering this batch's files.

## 21/8/26 (Feedback Sprint: UI feedback pass three)

A four-point follow-up. The user explicitly corrected an assumption — UI libraries are welcome, not off-limits — so `CLAUDE.md`'s tech-stack table (which had recorded "no UI library" as a project decision, not a user request) was corrected, and `framer-motion` was added, scoped specifically to animation rather than a wholesale styling-framework swap. Removed the dashboard's "Jump to a place" quick-nav row and its CSS, since the sidebar already covers the same destinations permanently. Reverted the four status colours to the exact original green/yellow/red/purple values (pulled from the last real git commit) in both themes, undoing the previous pass's severity-ramp re-hue at the user's direct request — the indigo primary colour is untouched. Rebuilt the four dashboard stat tiles: pulled the shared logic into a new `AttentionPanel.tsx`, used by both `Dashboard` and `PlaceDashboard`, so clicking a tile now expands that status's full list in place (capped at 10 rows plus a "See full list" button) with a framer-motion crossfade and height animation, replacing the previous pass's "click navigates to a new page" behaviour. Full reasoning in `docs/decisions.md`, "Feedback Sprint: UI feedback pass three." Verified via `tsc`/`eslint`/`npm run build` all clean (noting the bundle-size increase from the new dependency as an accepted tradeoff) plus a rebuilt Playwright harness confirming the panel expands/swaps/collapses correctly with no page navigation, scoped correctly on a place-dashboard, and rendering correctly in dark mode — zero unexpected console errors. Still to confirm: the user's own read on the animation feel, which needs the running app rather than a screenshot.

## 21/8/26 (Feedback Sprint: UI feedback pass four)

A six-point follow-up. Extracted `ItemList.tsx`'s item-detail modal and its open/edit/delete state machine into a new shared `components/ItemDetailModal.tsx` and `hooks/useItemDetail.ts`, so `AttentionPanel`'s rows (used by both `Dashboard` and `PlaceDashboard`) can open the exact same modal a card or table row does, rather than a separate copy — its rows are now real clickable buttons wired to that shared hook. Capped and centred `.dash-grid` at `max-width: 1100px`, scoped to the dashboard grids only, since `ItemList`'s own table still wants the full width. Rebuilt `Donut.tsx` as a real SVG ring — gapped, rounded-end segments, a staggered mount animation, and hover-linked ring/legend highlighting with a crossfading centre — replacing the old flat CSS conic-gradient disc, and redesigned `BarChart.tsx` to match with a grow-in mount animation, a subtle background rail behind each bar, a baseline rule, and the same point-at-one-dim-the-rest hover behaviour. Fixed the stat-tile hover bug (the tiles sit flush with no gap, so the previous `whileHover` lift revealed the row's edge underneath and dragged the active ring away from the tile) by moving hover feedback entirely into CSS — a background-colour change plus a small scale on the value text, neither of which moves the tile. Fixed the active ring not following the tile's rounded corner by giving the first/last stat tiles their own explicit `border-radius` matching the row's, since they'd only ever looked rounded via the row's outer clipping, not because the tile itself curved. Full reasoning in `docs/decisions.md`, "Feedback Sprint: UI feedback pass four." Verified via `tsc`/`eslint`/`npm run build` all clean — catching one real bug along the way, a JSX comment in `AttentionPanel.tsx` closed with `*/` instead of `*/}`, leaving its expression container unclosed — plus a rebuilt Playwright harness at a 1920px-wide viewport confirming the clickable attention rows open the shared modal, the donut/bar-chart hover interactions work, the stat-tile hover no longer shifts or gaps, the active ring now curves on the outer tiles, and the dashboard grid's centring holds up under its actual computed layout (not just a screenshot) — zero unexpected console errors. Still to confirm: the user's own visual check on their machine.

## 21/8/26 (Feedback Sprint: UI feedback pass five)

A one-line follow-up: `ItemList.tsx`'s `displayMode` now defaults to `'table'` instead of `'card'`, so list view loads first everywhere `ItemList` is reached from. Nothing else about the toggle or the list itself changed. Verified via `tsc`/`eslint`/`npm run build` all clean.

## 21/8/26 (Feedback Sprint: UI feedback pass six)

A screenshot flagged "too much empty space" on the dashboard's status-breakdown and items-by-location cards. Measured the real layout in a disposable harness before changing anything: the donut card's box was already exactly as tall as its content plus padding, so the actual issue was `.donut-row` having no `justify-content`, leaving the ring+legend hugged to the card's left edge with everything past them empty on a wide card — unlike the bar chart card, whose columns already stretch to fill via `flex: 1`. Fixed with `justify-content: space-between; width: 100%` on `.donut-row` (anchoring the legend to the card's right edge, with a column-direction override for the existing mobile breakpoint) and a bump to the dashboard's donut size (140px → 168px) so its card height better matches the bar chart card's. Full reasoning in `docs/decisions.md`, "Feedback Sprint: UI feedback pass six." Verified via `tsc`/`npm run build` clean and `eslint src` clean, plus re-measuring the harness's actual computed layout (not just a screenshot) to confirm the fix.

## 21/8/26 (Feedback Sprint: UI feedback pass seven)

A three-point follow-up: fill the gap below the dashboard's charts, make the "Needs attention" panel always fill the screen and scroll internally instead of expanding/collapsing, and make the "Items by location" bar chart look nicer with nicer colours. Built a CSS flexbox/grid stretch chain (`.app-main-fill { height: 100vh }`, applied only to the two dashboard views in `App.tsx`, propagating `flex: 1; min-height: 0` all the way down to each chart card and the attention panel) so both columns now genuinely fill the viewport with no leftover gap. Restructured `AttentionPanel.tsx` to drop its old height-animating `layout` prop and 10-row "and N more" cap in favour of a fixed-height card whose `.attention-list` scrolls internally, with the "See full list" button pinned at the bottom. Checked the bundled dataviz skill's reference palette and found the existing storage-location colours already byte-identical to its validated 3-series set, so rather than re-hue them, `BarChart.tsx` now renders each bar as a `color-mix()` gradient with a hover brightness lift — same colours, richer rendering. Full reasoning in `docs/decisions.md`, "Feedback Sprint: UI feedback pass seven." Verified via `tsc`/`eslint`/`npm run build` all clean, plus a rebuilt Playwright harness that measured the real computed layout (not just a screenshot) at desktop, tablet, and phone widths — catching one real bug along the way (`.attention-panel-content` using `height: 100%` instead of `flex: 1`, which silently failed to resolve and let the page grow instead of scrolling) before it shipped.

## 21/8/26 (Feedback Sprint: UI feedback pass seven — bug fix)

A follow-up screenshot showed the dashboard's four stat tiles with their numbers clipped partway down ("top cards are broken"). Cause: `.stat-row`'s pre-existing `overflow: hidden` (for its rounded-corner clipping trick) disables a flex item's usual protection against shrinking below its own content height — so once `.dash-grid` got a definite capped height this pass, `.stat-row` absorbed 100% of any shortfall between it and `.attention-card` by being silently squashed, rather than the shortfall landing on the flexible attention panel as intended. Confirmed via the same Playwright harness that this happened at every viewport height tested, not just short ones. Fixed with `flex-shrink: 0` on `.stat-row`. Full reasoning in `docs/decisions.md`. Verified via `tsc`/`eslint`/`npm run build` all clean, plus re-measuring the harness at six viewport heights (600–900px) confirming the tiles now render at their true height with the attention panel absorbing any shortfall instead.

## 21/8/26 (Feedback Sprint: UI feedback pass eight)

Direct feedback plus a mobile-table screenshot: wanted dark mode as the default for first-time visitors, and the list-view rows read as noticeably more spaced-out than their content needed. `useTheme.ts`'s no-stored-preference fallback changed from following the OS light/dark setting to always `'dark'` (anyone who has ever toggled the theme keeps that exact choice); `index.html`'s flash-prevention script updated to match. Measured the actual `.item-table` cell rule before touching it — the extra space traced to `body`'s global `line-height: 1.5` cascading into table cells uncorrected, not the padding — and added a scoped `line-height: 1.2` to `.item-table th, .item-table td`. `tsc -b --force`/`eslint src` both run directly on the user's machine via the device bridge, passed clean.

## 21/8/26 (Feedback Sprint: UI feedback pass eight — follow-up)

The line-height fix wasn't enough ("the gap is still there"), and the browser notification was flagged as too wordy. Rather than guess a second time, built a disposable Playwright harness reproducing the real `.item-table` markup and measured actual row heights at a phone viewport — confirmed the padding, not line-height, was the dominant remaining factor, and cut `.item-table` cell padding and `.status-badge` padding directly (measured rows: ~54px → ~40–49px). Separately, `useExpiryNotifications.ts`'s notification body switched from a comma-separated name list (capped at 5 + "and N more") to a one-line sentence — the item's own name if there's exactly one, otherwise just a count. `tsc -b --force`/`eslint src`, both run on the user's machine via the device bridge, passed clean. Still to confirm: the user's own look at both on a real device.

## 21/8/26 (Auth confirmation link pointing at localhost — fixed again)

A new user's email confirmation link pointed at `localhost` instead of the live site — the same class of bug fixed once already on 18/8/26 by pointing the Supabase dashboard's Site URL at the Vercel domain, which had evidently drifted back to `localhost` (or never durably saved) since `Auth.tsx`'s `signUp` call never specified its own redirect. Fixed at the code level this time so it doesn't depend on that dashboard setting staying correct: `signUp` now passes `options: { emailRedirectTo: window.location.origin }`. `tsc -b --force`/`eslint src` passed clean; not browser-tested against a real confirmation email. Still outstanding: confirming the Vercel domain is actually present in the Supabase project's Auth → URL Configuration → Redirect URLs allow-list, without which `emailRedirectTo` is silently ignored.

## 22/8/26 (Feedback Sprint 2)

A second round of feedback, ten items, with an explicit process request to commit after each one individually rather than as one batch — see the ten separate `Feedback Sprint 2 (N/10)` commits. Fixed a real timezone bug behind the false "date must not be in the future" error (comparing a UTC-parsed `Date` against local midnight — wrong for any UTC+ timezone) via a shared `todayLocal()` helper. Redesigned the "opened?" control as one label-plus-checkbox row. Fixed an SVG rendering quirk where a 0-count donut segment still drew a visible dot (`strokeLinecap="round"` on a zero-length arc). Added a "Fresh" stat tile to both dashboards. Added a full new "Microwave Meals" category end-to-end (enum, dropdowns, icon, keyword guess rule, and its own adjusted-expiry rule — unopened must stay in the freezer, opened must go to the fridge). Changed unsafe storage combinations from a warning-then-allow to a hard block on save, with the blocking message naming the correct location; removed the now-dead "Unsafe" stat tile/donut segment/filter option from both dashboards and the list (the underlying badge machinery stays, as a defensive fallback for pre-existing data). Added a hover-reveal slide animation to the add-item FAB. Made both dashboard charts' segments clickable, mirroring what the matching stat tile already does. Fixed the mobile donut legend to sit beside the ring instead of stacking below it. Every item verified individually with `tsc -b --force`/`eslint src` on the user's own machine via the device bridge before being committed. Still outstanding: running the category's `add value` migration was confirmed done by the user, but the FAB animation feel and the mobile legend's real-device fit are both things that read differently live than from source.

## 22/8/26 (Rename "Frozen Meals" to "Microwave Meals")

Same-day follow-up: renamed the category the sprint above had just added. Since the `add value` migration had already been run against the live database, used `alter type food_category rename value 'Frozen Meals' to 'Microwave Meals'` (a new migration) rather than adding a second enum value and backfilling every row — the rename relabels the existing value in place, so already-saved items carry the new name forward with nothing to backfill. Every matching code reference renamed to match (the `FoodCategory` type, all three category dropdowns, the keyword-guess rule, the icon lookup, and `adjustedExpiry.ts`'s switch case and its `evalFrozenMeals` function, renamed to `evalMicrowaveMeals`). `tsc -b --force`/`eslint src` passed clean. Still outstanding: running the rename migration in the Supabase SQL Editor — until then the live database's enum still says "Frozen Meals" while the app's code says "Microwave Meals," so saving that category will fail until it's run.

## 22/8/26 (Documentation catch-up — Feedback Sprint 2)

Audited every doc this project treats as a living reference against the real current code state: `docs/09-iteration-log.md` had nothing past "UI feedback pass seven bug fix" despite the project's own convention that every concrete step gets an entry here; `CLAUDE.md`'s Key Components/Current State/Database Schema and `docs/04-data-model.md`'s category list/migrations table hadn't been updated since the same point; `docs/02-requirements.md`'s unsafe-combination requirement still described the old warn-and-allow behaviour instead of Feedback Sprint 2's block-on-save. Backfilled all four. No app code changed.

## 22/8/26 (Status-breakdown donut: closing the empty-card gap)

Two screenshots at a wide viewport showed the dashboard's status donut with its ring and legend pushed to opposite edges and a large empty strip between them. `.donut-row`'s `justify-content: space-between` (from UI feedback pass six) had assumed the card's own width would fill the gap the way the bar chart's bars stretch to fill theirs — that doesn't hold for a fixed-pixel-size ring plus content-sized legend text, neither of which can grow into a wide card. Switched to `justify-content: center`, treating the ring+legend as one unit so it gets symmetric breathing room on both sides regardless of card width, plus grew the legend's own type per the user's suggestion. Deliberately left the ring's pixel size and punched-centre text alone — both already close to their own size ceiling (a fixed-diameter circle rendered at every viewport including phone width). `tsc -b --force`/`eslint src` passed clean. Still outstanding: the user's own look at the fix on a real wide screen.

## 23/8/26 (Feedback Sprint 3 — five items)

A third round of feedback, five items, with the same "commit after each change" instruction as Feedback Sprint 2 — see the five `Feedback Sprint 3 (N/5)` commits. Four clarifying questions asked up front (2FA method/scope, what the profile block should do, how to build the tutorial), all answered with the recommended option. Built in dependency order (profile page and about page first, then 2FA, then the chart-highlight, then the tour) while keeping each commit's `(N/5)` label matching the item's position in the user's original list. The sidebar's profile block and logo — previously decorative `<div>`s — became real `<button>`s opening two new pages, `ProfilePage.tsx` and `AboutPage.tsx`; the name/initials logic they share was pulled out into a new `lib/userDisplay.ts` so the two can't drift. Opt-in two-factor authentication (TOTP) added entirely on Supabase Auth's own MFA API — no new table, no custom crypto — with enrollment/disable both gated behind a fresh verified code (`ProfilePage.tsx`'s `TwoFactorSettings`) and a sign-in-time challenge (`MfaChallenge.tsx`) wired into `App.tsx`'s top-level `App()`. Clicking a dashboard stat tile now also highlights the matching status-donut slice (`Donut.tsx`'s new `activeKey` prop, merged with its existing hover state). A new custom-built `OnboardingTour.tsx` (framer-motion, no new dependency) walks first-time accounts through 5 anchored UI elements, gated on the account's own `user_metadata.has_seen_tutorial` rather than `localStorage` — matching the same account-not-browser reasoning behind the 2FA opt-in. Two `eslint-plugin-react-hooks` findings (`set-state-in-effect`) caught and fixed by restructuring rather than suppressing, one of which also fixed a real UX bug: the MFA gate's state is now derived at render time (tagged by account id) instead of reset by an effect on every session change, which would otherwise have flashed the app back to a loading screen on every plain token refresh. Every item verified individually with `tsc -b --force`/`eslint src` on the user's own machine via the device bridge before being committed. Still outstanding: the user's own live testing of 2FA enrollment/sign-in and the onboarding tour's on-screen positioning, and `git push origin main` for all five commits.
