# Integration tests

Tests against a real Supabase schema — RLS enforcement, constraint checks (e.g. `date_opened_required_when_opened`). See `docs/08-test-plan.md`. Empty until there's a schema-backed feature worth testing against.
