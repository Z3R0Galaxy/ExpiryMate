# Edge Functions

Reserved for the Slice 5 expiry-notification email digest (a Supabase Edge Function that sends a daily/periodic email for items nearing their adjusted expiry date, as an optional escalation beyond the browser `Notification` API). No functions exist yet — see `docs/decisions.md` (Slice 5) and `docs/08-test-plan.md` for how this will be tested once built.
